{
  "Stable": [
    {
      "version": "5.3.3",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v5.3.3/Shoko.CLI_Framework_any-x64.zip",
      "commit": "892461188e5f1cc3ccf1b9b6eb1d4dfd31bea6f8",
      "tag": "v5.3.3",
      "date": "2026-03-27T04:02:59+01:00",
      "releaseNotes": ""
    }
  ],
  "Dev": [
    {
      "version": "6.0.0.476",
      "tag": "v6.0.0-dev.476",
      "commit": "4dc1ceee496af1a491f803b4712d7d0d0fa2c245",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.476/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-19T09:14:00Z",
      "releaseNotes": "\n`feat` (`abstractions`): **Record the absolute path on the video file events**:\n\n`VideoFileDetectedEventArgs` is raised before any record exists for the\nfile, so its absolute path was only reachable by reaching into `FileInfo`,\nor by joining the managed folder's `Path` to `RelativePath` later against a\nfolder that may have been reconfigured since. The other file events carried\nit only through `File.Path`, beside a `RelativePath` they already lifted to\nthe top level.\n\n- Added `Path` to `VideoFileDetectedEventArgs`, taken from\n  `FileInfo.FullName` when the event is built\n- Added `Path` to `VideoFileEventArgs`, taken from `File.Path`, so the\n  hashed, relocated and deleted events spell it the same way\n- Named `System.IO.Path` explicitly wherever the new property would\n  otherwise shadow it, including in `VideoFileRelocatedEventArgs`\n\nNo constructor changed. `VideoFileRelocatedEventArgs.PreviousPath` already\nexisted and is left as it was.\n\n`chore`: **Sort usings**.\n\n`feat` (`plugin`): **Tell each plugin once every plugin has been set up**:\n\n`Setup` runs in load order, and a dependency always loads before the plugins\nthat depend on it, so a plugin that owns something other plugins contribute\nto is set up before any of its contributors. Freezing a registry in its own\n`Setup` would freeze it empty.\n\n- Added `IPlugin.Ready()` with an empty default, called once after every\n  plugin's `Setup` and before the web host starts\n- Called it through `PluginManager.ReadyPlugins`, skipped when a plugin\n  already failed to set up\n- Recorded a throw the same way as a failed `Setup`: the web host still\n  comes up, and the boot stops with the plugin named\n\n`refactor` (`abstractions`): **Take the host's startup steps off the plugin-facing interfaces**:\n\n`AddParts` sat on eight service interfaces, and `ScanForPlugins`,\n`RegisterPlugins` and `InitPlugins` on `IPluginManager`, though only the host\never calls them. On the interfaces they read as the way to contribute, and\nthey were not: seven of the `AddParts` ignore every call after the first, so a\nplugin's call did nothing, and `IImageManager.AddParts` replaced its list, so a\nplugin's call dropped every resolver the other plugins contributed. Neither\nsaid so.\n\n- Removed `AddParts` from `IConfigurationService`, `IImageManager`,\n  `IMetadataService`, `IAiringScheduleService`, `IVideoService`,\n  `IVideoReleaseService`, `IVideoHashingService` and\n  `IVideoRelocationService`, and `AddTransformParts`/`AddObserverParts` from\n  `IVideoStreamPipelineService`\n- Removed `ScanForPlugins`, `RegisterPlugins` and `InitPlugins` from\n  `IPluginManager`\n- Resolved each service through its interface in `PluginManager` and handed it\n  its parts only when it is the implementation that takes them, and did the\n  same for `RegisterPlugins` in `Startup`, so the registered service types are\n  unchanged\n- Gave `ImageManager.AddParts` the once-only guard the others have\n- Said in the plugin README that implementing a contract is the whole of it\n\n`fix` (`release`): **Enqueue the rescan that `TryScheduleRescanForVideo` builds**:\n\nThe method saved a new match attempt and built the provider chain, then\nreturned `true` without submitting it. `ScanForMissingReleaseInfoJob`\ntherefore never rescanned anything, and every sweep still inflated the\nvideo's `AttemptCount`.\n\nThe chain is submitted with a plain `Enqueue` rather than\n`EnqueueAfterCurrent`, since the caller is a recurring sweep over many\nvideos and each rescan jumping the queue would crowd out everything else.\n\n`fix` (`release`): **Hand `SearchCompleted` the release a queued search found**:\n\n`FinalizeReleaseSearchJob` raised the event without the release, so on the\nqueued path `ReleaseInfo` was always `null` and every search looked\nunmatched to a subscriber, successful or not. The provider jobs have\nalready saved what they found, so a successful attempt's release is the\nvideo's current one, and that is what is passed now.\n\n`fix` (`plugin`): **Set up every plugin before failing, rather than stopping at the first**:\n\n`SetupPlugins` and `ReadyPlugins` rethrew at the first plugin that failed,\nso every plugin after it in load order never got `Setup`, and none got\n`Ready`. The web host still starts after a recorded failure, so those\nplugins' hosted services and middleware then ran assuming state they never\nreceived.\n\n- Ran each step for every active plugin and collected the failures, so only\n  the plugins that actually threw are left unset\n- Threw one `AggregateException` naming every failing plugin, instead of the\n  first alone\n- Corrected the remark on `SetupPlugins`, which still said it runs after the\n  web host is up\n\n`docs` (`plugin`): **Document `Setup` and `Ready`, and the order a plugin starts in**: (_Skip CI_)\n\nNo README mentioned `IPlugin.Setup` or `IPlugin.Ready`, and two still said the\nclass implementing `IPlugin` takes constructor injection, which stops a plugin\nloading at all. The start-up order was undocumented and partly wrong.\n\n- Added `Setup` and `Ready` to the overview, and a start-up order table with\n  what is safe at each step, including that exported contract implementations\n  are built before the database is open\n- Said what a throw from either leaves running: every plugin still gets its\n  call, and the web host, its application hooks and every hosted service still\n  start\n- Corrected the injection claims in `Metadata/Services` and `Core/Services`,\n  the \"first point with a built container\" claim, the \"no init hook\" claim in\n  `Web/Services`, and where the registration hooks are looked up\n- Warned that an unregistered class implementing two contracts is built twice,\n  and that the plugin class is never the instance a contract resolves to\n- Named the `Shoko.QueueProcessor` package in place of the repository's\n  `CLAUDE.md`, and the `503`/`400` start-up gates a plugin controller is behind\n- Added the load-bearing `ExcludeAssets=\"runtime\"` to the `shoko-build`\n  example `.csproj`\n\n`docs` (`plugin`): **Say which providers start disabled, and fix the unmatched-file example**:\n\n- Said that a new release provider starts disabled unless its `Name` is\n  `AniDB`, and that a plugin airing provider starts with no kinds enabled,\n  where both used to be silent; a provider that never runs reads as a\n  registration problem otherwise\n- Said that a release provider that throws aborts the rest of the chain and\n  skips the finalizing step\n- Rewrote the unmatched-file example around `SearchCompleted`, now that it\n  carries the found release, and said why `VideoFileHashed` cannot answer it\n- Dropped the note that the rescan never runs, now that it does\n- Said that playback observers run one after another, awaited and\n  uncancellable, before the response is sent\n- Documented on the event arguments that `RelativePath` always starts with a\n  separator, which `Path.Combine` mishandles\n\n`docs` (`plugin`): **Correct hash ownership, the repeating `VideoFileDetected`, and the image reverse lookup**: (_Skip CI_)\n\n- The first provider in order keeps a hash type, so enabling one elsewhere does nothing until the holder drops it\n- `VideoFileDetected` is raised again on every scan until a location exists, from several threads\n- A plugin entity always reaches the resolvers on the way back, so `GetEntity` is needed even when a core arm handles the way in\n\n`docs` (`plugin`): **Say which thread events are raised on, and that scheduled metadata updates jump the queue**: (_Skip CI_)\n\n- Configuration `Saved` and every image event run on an unobserved thread-pool task, so a handler's exception is lost and image events have no order\n- Airing events run on the writer's thread, and a throwing handler fails the write that already stored its rows\n- The AniDB, TMDB and AniList `Schedule…` updates go through `RunAfterCurrent`\n\n`docs` (`plugin`): **Document secret masking, and the action rules that fail startup**: (_Skip CI_)\n\n- Added a Secrets section to the configuration README: what counts as one, how the sentinel round-trips, `SerializeWithMasking`, `MaskSecrets` and `RestoreMaskedSecrets`, and why a list holding one needs `[Key]`\n- A scoped action has to derive directly from its base class, and breaking either rule takes the whole startup down\n- Actions are found only as public types in the main assembly, and the startup probe and `Validate` resolve them from the root container\n- An `internal` configuration class makes `Load()` throw\n\n`fix` (`user`): **Throw `ArgumentNullException` from `DeleteUser` for a null user, as documented**.\n\n`docs` (`abstractions`): **Correct stale XML docs found by the plugin docs audit**:\n\n- `skipEvents` on `IVideoReleaseService` said `false` skips; it is `true`\n- `DeleteVideoFile`'s `removeFile` said the reverse of what it does, and defaults differently from its siblings\n- `IConfigurationService` `Load`/`Save` never throw for a base configuration; they return a default instance and `false`\n- `DeleteUser` faults with `GenericValidationException` for the last admin, and ignores a user that isn't stored\n- `ISupplementaryMetadataProvider` is called during the refresh, and from saved releases before the anime is cached; `OnSeriesRemoved` has no caller\n- `IPluginServiceRegistration` and `IPluginApplicationRegistration` never construct an instance\n\n`fix` (`config`): **Match masked secrets in lists by a keyed fingerprint instead of `[Key]`**:\n\n`[Key]` is a display label in the WebUI: it can repeat and gets renamed, so\npairing list elements by it handed one element's secret to another whenever\ntwo labels collided, and lost the secret when a label changed.\n\n- Masked secrets now go out as `***SECRET-UNCHANGED:<fingerprint>***`, an HMAC of the value under a random per-install key\n- Inside a list, a masked value is matched by fingerprint against the secrets stored for the same property, whatever element they sit on\n- The key lives in `configuration-secrets.key` in the data directory, kept out of the settings file so a shared settings file cannot be used to test guesses offline\n- `ConfigurationSecrets.Mask` and `Restore` take the key, and `IsMasked` recognises either form; the bare `Sentinel` is still accepted outside lists\n\n`chore` (`abstractions`): **Bump to `6.0.0-alpha.92`**:\n\n`Path` on the video file events and `IPlugin.Ready` only reach a plugin that\ncompiles against a version declaring them, and the members taken off the\nservice interfaces and `IPluginManager` break any plugin still calling them,\nso both need a version of their own.\n"
    },
    {
      "version": "6.0.0.475",
      "tag": "v6.0.0-dev.475",
      "commit": "7c7f38478ab58bc1aebf050330419662e6deae8d",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.475/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-18T12:43:25Z",
      "releaseNotes": "\n`fix` (`plugin`): **Set a plugin up before the database, and record the failure instead of throwing**:\n\n`SetupPlugins` ran from `LateStart`, which is after the database is opened,\nand let the exception reach that handler. Both were wrong. A plugin should\ntake its services while the rest of the server is still ahead of it, and\nthrowing out of the start-up path skips the web host and lets the process\ndie with nothing left running to report why.\n\nThe failure is recorded where it happens and the boot is abandoned once the\nweb host is listening, so the server stays reachable in a state that names\nthe plugin that stopped it.\n\n- Moved the call to right after `InitPlugins`, before any database work\n- Recorded `StartupFailedException` rather than throwing, and returned the\n  host once it is up instead of continuing into setup mode or `LateStart`\n- Corrected the contract, which said `Setup` runs after the server is up\n\n`chore` (`abstractions`): **Bump to `6.0.0-alpha.91`**:\n\n`IPlugin.Setup`'s documentation changed after `alpha.90` was cut, and the XML\ndocs ship in the package, so a plugin author reading it needs a version that\ncarries the corrected text.\n"
    },
    {
      "version": "6.0.0.474",
      "tag": "v6.0.0-dev.474",
      "commit": "6bd003e5d188d44fa92b4af1decbf9ca21a916d7",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.474/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-18T11:36:19Z",
      "releaseNotes": "\n`fix` (`plugin`): **Make a failed `Setup` fatal once the web host can report it**:\n\n`Setup` ran from `InitPlugins`, which happens before the web host starts and\nbefore the hosted services a plugin registered, and a plugin that threw was\nskipped. That skip could not work: services are registered into the container\nbefore initialization and cannot be taken back out of a built one, so a\nskipped plugin's hosted services still started. `Shoko.Plugin.Mcp` proved it\nby taking the whole server down with a `NullReferenceException` inside\n`ConfigurationService.Load`, because the configuration type it reached for\nbelonged to the plugin that had just been dropped from the exported types.\n\nThe fault has to be fatal, then, since it cannot be contained. Running it\nlate makes it a fatal the operator can read: the web host is already\nlistening, so `StartupFailedException` reaches the Web UI and names the\nplugin instead of the process dying with a stack trace pointing at\nconfiguration.\n\n- Moved the call into `PluginManager.SetupPlugins`, invoked from `LateStart`\n  after the session factory is up\n- Let the throw reach `LateStart`'s handler, wrapped with the plugin's name\n- Dropped the skip, and with it the disposal that only made sense while a\n  plugin could be dropped and the server carry on\n- Said in the contract that `Setup` runs after the server is up, that\n  throwing stops startup, and that the hook only binds on recompile\n"
    },
    {
      "version": "6.0.0.473",
      "tag": "v6.0.0-dev.473",
      "commit": "cda235da0ed0f5fdc36917a3bcd68ea7266b634e",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.473/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-18T11:13:35Z",
      "releaseNotes": "\n`docs`: **Say where `PocoCache` actually lives**: (_Skip CI_)\n\n`NutzCode.InMemoryIndex` stopped being a dependency in `b62734299`, which\nmoved `PocoCache` and `PocoIndex` into `Shoko.Server.Utilities`. The note\nstill named the package, which is the import that broke `master` when a\nbranch written against the old description merged.\n\n`feat` (`plugin`): **Let a plugin acquire its services after the container exists**:\n\nA plugin is built twice, and only the second build goes through the\ncontainer. Discovery builds it with `Activator.CreateInstance` to read its\nidentity, which needs a public parameterless constructor and throws without\none, so a plugin that took a constructor dependency never loaded at all and\nfailed with a `MissingMethodException` that named nothing useful.\n\n`Setup` is the way in instead, mirroring `IQueueJob.Setup`, which exists for\nthe same reason. The default implementation is a no-op, so nothing already\nwritten has to change.\n\n- Added `IPlugin.Setup(IServiceProvider)` with an empty default\n- Called it from `InitPlugins` on the instance the container built\n- Skipped a plugin whose `Setup` threw, and disposed it if it can be, rather\n  than leaving one loaded that never got what it asked for and would fail\n  later somewhere that points nowhere near here\n\nNote that a plugin only gets this on recompile: a `Setup` method compiled\nagainst an abstraction that did not declare one is an ordinary public method\nthe runtime never maps, so it silently keeps the default. That also means no\nexisting plugin is hijacked by the new member.\n\n`chore` (`abstractions`): **Bump to `6.0.0-alpha.90`**:\n\n`IPlugin.Setup` only reaches a plugin that compiled against an abstraction\ndeclaring it, so the hook needs a version of its own before any plugin can\ntake it up.\n"
    },
    {
      "version": "6.0.0.472",
      "tag": "v6.0.0-dev.472",
      "commit": "0266f5d2d0cf3458986281706333c2d942e02e61",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.472/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-18T06:17:33Z",
      "releaseNotes": "\n`repo` (`workflows`): **Fail when a plugin download url cannot be resolved**: (_Skip CI_)\n\nBoth build workflows resolved a download URL with an unchecked `curl -s`\npiped into `jq`, then curled whatever came out. A rate-limited or erroring\nAPI returns a body with no `.assets`, `jq` yields the string `null`, and the\nnext line fetched that literally: a run failed four seconds in with\n`curl: (6) Could not resolve host` while the release it wanted existed and\nhad its asset attached.\n\n- Resolved with `--fail --silent --show-error`, so an error body never\n  reaches `jq`\n- Refused an empty or `null` URL with a message naming what could not be\n  resolved, rather than passing it to `curl`\n- Authenticated the GitHub API call with `GITHUB_TOKEN`, so a shared runner\n  hitting the unauthenticated rate limit is no longer a build failure\n- Downloaded with `--fail`, so a non-2xx response cannot be written into the\n  zip and surface later as a corrupt archive\n\n`misc`: **Unify missing episode definition and refresh AniDB group status periodically** ([#1421](https://github.com/ShokoAnime/ShokoServer/pull/1421)):\n\n* fix(anidb): refresh group status during periodic anime updates\n\n`AniDB_GroupStatus` was only refreshed by the legacy v1 API, so with a\nv3-only install the cached release-group progress stopped updating and\n\"released-but-absent\" missing-episode counts under-counted.\n\n- Enqueue `GetAniDBReleaseGroupStatusJob` for each updated series in\n  `GetUpdatedAniDBAnimeJob`, propagating `ForceRefresh` so scheduled\n  runs keep the `ShouldSkip()` ended-anime gate intact\n\n* fix(api): unify missing episode definition behind shared predicate\n\n`/v3/Series` → `Sizes.Missing.Episodes` used an aired-but-absent check\nwhile `/v3/MissingEpisodes` and the persisted counts used a\nreleased-but-absent check via `AniDB_GroupStatus`, so the same series\ncould report different missing counts.\n\n- Added `AnimeEpisodeExtensions.IsMissingEpisode` as the single source\n  of truth: aired, not hidden, no local file, and either no group\n  status data or a group with `CompletionState` complete/finished or\n  `LastEpisodeNumber` past the episode number\n- Applied the group status gate to `Sizes.Missing.Episodes` in both\n  `GenerateSeriesSizes` and `GenerateGroupSizes` so series and group\n  counts agree; `GenerateSeriesSizes` now requires the per-anime\n  status dictionary so future callers cannot fall back to the\n  divergent aired-but-absent logic\n- Replaced the non-collecting `GetMissing` raw SQL with cache-based\n  filtering through the shared predicate; the collecting variant\n  stays as raw SQL\n- Added `AniDB_GroupStatusRepository.GetByAnimeIDs` with batched\n  (1000-per-query) lookups\n\n* refactor(api): pass AniDB episode explicitly to missing predicate and fix line length\n\n- `IsMissingEpisode` now takes the `AniDB_Episode` as a parameter so\n  `GenerateSeriesSizes` and `GetMissing` reuse the already-resolved\n  AniDB episode instead of re-reading `episode.AniDB_Episode`\n- wrapped the `GenerateSeriesSizes` signature to respect the 160-char\n  line limit\n\n* refactor(anidb): cache AniDB_GroupStatus repository to fix N+1 queries\n\nConverted `AniDB_GroupStatusRepository` from a direct repository to a\ncached one with a `PocoIndex` on `AnimeID`. `GetByAnimeID` and\n`GetByAnimeIDs` now serve from the in-memory cache, so building Series\nDTOs and other group-status lookups no longer hit the database once per\ncall. `DeleteForAnime` deletes through the base repository so the cache\nstays in sync, and still schedules `RefreshAnimeStatsJob` afterwards.\n\n* refactor(api): move missing-episode predicate to AnimeEpisode and fix sonar warnings\n\n- moved `IsMissingEpisode` from `AnimeEpisodeExtensions` onto the\n  `AnimeEpisode` model per review feedback, so it no longer resolves\n  the AniDB episode redundantly and the extension file is removed\n- removed redundant null-forgiving operators flagged by Sonar\n  `S8969` in `AnimeEpisodeRepository.GetMissing`\n\n`fix`: **Add missing ISO 639 language mappings and fix subtitle-filename language parsing** ([#1442](https://github.com/ShokoAnime/ShokoServer/pull/1442)):\n\n* fix(abstractions): add missing ISO 639 language mappings and IETF fallback\n\n`LanguageExtensions.GetTitleLanguage()` was missing 18 codes observed in\nproduction logs (`os`, `ia`, `hz`, `ee`, `ie`, `kr`, `ng`, `enm`, `cu`,\n`lg`, `cv`, `an`, `bo`, `io`, `kl`, `li`, `nd`, `ss`), each logged as an\n`Unrecognized language string` error every time it was encountered.\n\nAlso added a generic fallback for \"xxx-XX\"-shaped IETF tags (e.g.\n`cmn-CN`) that the existing 2-letter+2-letter fast path didn't cover —\nstrips the country suffix and retries on the language part alone before\nreporting unknown.\n\n* fix(core): don't leak garbage text as a language from subtitle filenames\n\n`SubtitleHelper.GetLanguageFromFilename()` only recognized the\n`basename.eng.srt` dot-separated convention. Two problems:\n\n- External subtitles named `basename_eng.ass` (underscore, not dot) —\n  the actual convention used by some release groups, including\n  duplicate-track suffixes like `_eng_1` — weren't recognized at all.\n- When a title happened to contain a literal `...` (ellipsis), the dot\n  count coincidentally satisfied the \"at least 3 dot-parts\" check, and\n  the unrecognized-segment fallback returned that raw multi-word text\n  as if it were a language string, which then got logged repeatedly as\n  an unrecognized language.\n\nAdded underscore-suffix parsing (with duplicate-track disambiguator\nsupport) tried first, and made the dot-based fallback return null for\nsegments `GetLanguageFromName` doesn't recognize instead of passing\nthe raw text through.\n\n`fix` (`config`): **Mask a secret marked with `[DataType]` too**:\n\n`[DataType(DataType.Password)]` renders a password element now, but masking,\nthe omission guard and the settings dump all keyed off `[PasswordPropertyText]`\nalone. A property the user is shown stars for went out in plaintext on the v3\nconfiguration surface and into the log.\n\n- Treated either marker as a secret in `ConfigurationSecrets`, so masking\n  follows whatever renders as a password\n- Redacted both in `SettingsProvider.DumpSettings`\n- Taught the omission guard both markers, so a credential marked the\n  data-annotations way counts as marked rather than as an offender\n\n(cherry picked from commit d988145bc63ee5e05903a9394096fa4daab70144)\n\n`fix` (`repo`): **Import `PocoIndex` from where it now lives**:\n\n`AniDB_GroupStatusRepository` arrived importing `NutzCode.InMemoryIndex`,\nwhich no project references any more: `PocoCache` and `PocoIndex` are\nvendored in `Shoko.Server/Utilities/PocoCache.cs` and every other cached\nrepository imports them from `Shoko.Server.Utilities`. The stale import\nleft `master` unable to build.\n"
    },
    {
      "version": "6.0.0.471",
      "tag": "v6.0.0-dev.471",
      "commit": "9f884d0d5a4ad624d83b19720ad1d7143b5adf23",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.471/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-18T03:04:10Z",
      "releaseNotes": "\n`chore`: **Tidy usings in three files**:\n\nRan `scripts/sort_usings.py`, which dropped an unused\n`Shoko.Server.API.Authentication` import from `SystemService` and put the blank\nline the script expects before the alias usings in all three.\n\n`refactor`: **Move `PocoCache` into the `Shoko.Server.Utilities` namespace**:\n\n`PocoCache` was the only file left declaring `NutzCode.InMemoryIndex`, a\nnamespace from the code's original author rather than anything this repo ships.\nIt now lives in the namespace its folder implies, and the 58 files that used it\nimport `Shoko.Server.Utilities` instead. The MIT header naming the original\nauthor is untouched.\n\n`feat` (`abstractions`): **Add the airing schedule contract**:\n\nAdded the `Shoko.Abstractions.Metadata.Airing` namespace and\n`IAiringScheduleService`, the plugin-facing contract for airing\nschedules: providers push what they find through the service, and the\ncore only ever asks a provider to refresh an entity.\n\n- Provider side: `IAiringScheduleProvider` with its configuration\n  variant, `AiringScheduleProviderInfo`, `AiringKind`,\n  `AiringScheduleData`, `AiringTrackData`, `EpisodeAiringData`, the\n  partial update classes, `EpisodeAiringUpdateOptions` and\n  `IAiringScheduleEntityResolver`\n- Read side: `IAiringSchedule`, `IEpisodeAiring`, `IAiringTrack`,\n  `IAiringChannel`, `AiringChannelType`, the filtering options,\n  `AiringTrackPreference`, the event args and the refresh result types\n- `IAiringScheduleService` carries the channel helpers as statics, next\n  to the image manager's `GetIDForImageSourceAndResourceID`:\n  `ChannelIdentifierNamespace`, `GetChannelID`, `NormalizeChannelName`\n  and `GetRegionalChannelName`\n- Two exceptions of our own, `AiringScheduleValidationException` for\n  batch writes and `ChannelAliasConflictException` for a name another\n  channel already holds; everything else stays a plain argument or state\n  exception\n- `INetwork` carries what a TMDB network and an airing channel share, so\n  `ITmdbNetwork` gains it as a base, and `DataEntityType` gains\n  `Channel` as an alias of `Network`\n- Bumped the abstractions to `6.0.0-alpha.89`\n\n`refactor`: **Drop the AniList-only air time estimation**:\n\nBroadcast times move to the airing schedule service, so the stop-gap\nthat folded AniList schedule entries into the shoko episode is gone.\n\n- `AnimeEpisode`'s `IEpisode.AirDateWithTime` falls back to the AniDB\n  date and then the TMDB date, and `IEpisode.AirDate` follows it\n- Removed `IShokoEpisode.IsAirTimeEstimated`,\n  `AnimeSeries.AnilistAirTimeOffset` and\n  `ResetAnilistAirTimeOffset` with its two call sites\n- `Dashboard.Episode` loses `AiredAt`, `HasAirTime` and\n  `IsAirTimeEstimated`, and `CalendarEpisodes` loses the\n  `includeWithAirTime` query parameter that read them; the calendar now\n  orders by `AirDate`\n\n`refactor`: **Grow `AirTimeUtility` into `AiringScheduleUtility`**:\n\nRenamed `AirTimeUtility` to `AiringScheduleUtility` and gave it the whole\npure logic layer for airing schedules, with no repository or service in\nsight.\n\n- Derived keys for a schedule (`{channel or \"-\"}:{tracks, sorted}`), for an\n  airing (`{EpisodeSource}:{EpisodeID}`), and for a provider key that names a\n  whole slot\n- `InferAirings`, a pure function from (old airings, new airings, now) to the\n  rows to save and the rows to delete: key matching, re-key pairing,\n  corrections under 24 h, moves of 24 h or more recording `OriginalAiredAt`,\n  removed future airings becoming slotless, cause detection over consecutive\n  equal moves with link sets counting as one release, stale link unlinking,\n  and slotless retention by supersession and expiry\n- `LearnProfile` and `EstimateAiring`: the offset median per schedule, the\n  trailing shift, the anchor on the episode's earliest known Original airing,\n  the hiatus, and the coverage limits\n- `FindCadenceBreaks`, measuring gaps between releases rather than episodes,\n  so a double slot counts once and a season drop has no cadence\n- Plain records for what all of it takes and returns, in\n  `Shoko.Server/Utilities/Airing/`\n\n`feat` (`airing`): **Store schedules, airings and channels**:\n\nAdded the three tables the airing schedule service reads and writes,\nplus the cached repositories every read goes through.\n\n- `AiringSchedule`, `EpisodeAiring` and `AiringChannel` under\n  `Shoko.Server/Models/Airing/`, with NHibernate maps beside the\n  existing ones\n- `JsonListConverter<T>` for the JSON array columns, since\n  `StringListConverter` stores `|||` delimited text\n- `AiringScheduleUtility` gained the derived identifiers the models\n  need: `GetScheduleID`, `GetEpisodeAiringID`, `GetChannelID` and\n  `NormalizeChannelName`\n- `AiringChannelRepository` resolves a name through one index over a\n  channel's own name and all of its aliases\n- `ImageManager` now keys every `INetwork` rather than only an\n  `ITmdbNetwork`, and resolves `(Shoko, Channel)` through the channel\n  repository, so a channel can hold images\n\n`feat` (`airing`): **Add the airing schedule service**:\n\nImplemented `IAiringScheduleService` as `AiringScheduleService`, split over\npartial files for providers and channels, schedules, airings, reading and\nrefreshing. Providers push what they find through it and every change takes\nthe provider instance, checked by reference against what `AddParts` received\nand against the owner stored on the schedule.\n\n- `AddParts` mirrors `VideoReleaseService`, with provider ids from\n  `UuidUtility.GetV5(\"AiringScheduleProvider=…\", pluginInfo.ID)` and\n  `AiringScheduleServiceSettings` through `ConfigurationProvider<T>`. A first\n  run seeds the core plugin's own providers, keyed on their computed id, with\n  everything they declare; providers found later start disabled at the end\n- The channel registry: `FindOrRegisterChannel` is alias-blind and claims a\n  name from a conflicting alias with a warning, `AddChannelAliases` throws\n  `ChannelAliasConflictException` on a name another channel already answers\n  to, and the time-zone helpers normalise to IANA ids or a fixed `±HH:MM`\n- Writes run the delay inference, estimates and link-set logic already in\n  `AiringScheduleUtility`, with per-airing validation reported as one\n  `AiringScheduleValidationException` for a whole batch\n- Reads collect the episode's linked keys, filter, add each covering\n  schedule's estimate, de-duplicate channels by provider priority and order\n  by track preference, channel preference, real over estimated, time,\n  priority and channel name. Enrichment is lazy in the view types and every\n  read shares one `AiringReadContext`, so nothing resolves twice\n- `RefreshAiringScheduleJob` carries both the hint and the wait paths, keyed\n  by provider and entity, with `MaxConcurrentRefreshes` honoured by a\n  per-provider semaphore, and `AiringScheduleRetentionJob` runs the daily\n  sweep through `RecurringJobRegistry`\n\n`feat` (`airing`): **Add the AniList airing schedule provider**:\n\nAniList becomes the first source the airing schedule service carries.\n\n- `AnilistAiringScheduleProvider` declares `Original` as its only kind and\n  carries a refresh to `AnilistMetadataService.ScheduleUpdateOfAnime` for\n  every AniList anime the series keys to, returning `false` for a series\n  that keys to none\n- `AnilistMetadataService` writes the run to the service after the episode\n  rows are rebuilt: one channel-less schedule per anime keyed `original`,\n  with one `Original` track in the anime's `OriginalLanguageCode`, covering\n  episode 1 to the reported episode count and finished once the media\n  status is `FINISHED`, then `SetAirings` with one airing per episode\n  AniList knows a slot for\n- Purging an anime removes the schedules the provider owns for it\n- `AnilistLinkingService` invalidates the cached estimate profiles of the\n  shoko series whose links changed\n\n`feat` (`api`): **Add the airing schedule endpoints**:\n\nAdded `AiringScheduleController` with the airings range read, one airing\nand its link set, one schedule and its airings, the provider routes\nmodelled on `ReleaseInfoController`, the channel routes, the time zone\nlist, the two preference routes, and the series and episode routes\nincluding a `Refresh` that can wait on the queued work.\n\n- Purpose-built DTOs under `API/v3/Models/Airing/`, so a calendar row\n  carries no titles, images, file or user data it doesn't need\n- Display data is opt-in through `include`, resolved once per distinct\n  series where it can be\n- `TimeZone` goes over the wire as an object, so a client can render the\n  source's own local time without a second lookup\n- Wrote `API/v3/AiringSchedule.md` for client authors\n\n`refactor` (`api`): **Drop `Dashboard/CalendarEpisodes` for the airings endpoint**:\n\nRemoved `GetCalendarEpisodes` outright; `/api/v3/AiringSchedule/Airing`\nreplaces it with kinds, channels, tracks, estimates and delay gaps, and\nno compatibility shim is kept.\n\n- `Dashboard/AniDBCalendar` no longer delegates to it and runs its own\n  AniDB date query, so it is back to every episode type and AniDB date\n  order, and no longer moves when a broadcaster moves an episode\n- Kept the \"missing means nothing downloaded\" rule from e76ea8a83\n\n`feat` (`db`): **Add the airing schedule tables and seed them from AniList**:\n\nAdded `AiringSchedule`, `EpisodeAiring` and `AiringChannel` as the next\nschema version on all three backends, with the unique keys the service\nrelies on — `(ProviderID, SeriesSource, SeriesID, SeasonID, Key)` and\n`(AiringScheduleID, Key)` — plus the provider, series, channel, episode\nand link-head indexes. MySQL declares every text column `utf8mb4` with\nthe unicode collation up front, so the after-patch charset fix leaves\nthem alone.\n\n- Registered `DatabaseFixes.SeedAnilistAiringSchedules` as a post-fix\n  under the same version on each backend\n- The seed builds one schedule per AniList anime with broadcast times\n  and one airing per `Anilist_Episode` with an `AiredAt`, shaped the way\n  `AnilistMetadataService` would submit them, written straight to the\n  repositories in batches with progress on `SystemService.StartupMessage`\n- Rows whose keys already exist are skipped, so it is safe to re-run,\n  and airings the retention sweep would only remove again are left out\n- Made `AnilistMetadataService.AiringScheduleKey` internal, so the seed\n  and the provider key the same schedule\n\n`docs` (`airing`): **Add the plugin-facing README for airing schedule providers**:\n\nWritten like `Video/Release/README.md`: what an airing schedule is and isn't,\nthe channel/schedule/airing model, registering an `IAiringScheduleProvider`,\nthe `FindOrRegisterChannel` -> `AddOrUpdateSchedule` -> `SetAirings` ->\n`LinkAirings` write flow with a weekly-broadcast and a whole-season-drop\nworked example, the rules a provider must respect with the exception\ncontract table, and entity resolvers for plugin-owned series/seasons/episodes.\n\n`Shoko.Server/API/v3/AiringSchedule.md` already covers the client-facing side\n(written and kept current across the `feat(api)` and `refactor(api)` commits)\nand needed no changes after checking it against the implemented service and\ncontroller.\n\n`fix` (`airing`): **Stop reading repositories before the database is up**:\n\n`AiringScheduleService.AddParts` counted `AiringSchedule`, `EpisodeAiring`\nand `AiringChannel` rows in its startup log line. Parts are added from\n`PluginManager.InitPlugins()`, which runs before `RepoFactory` is\ninitialised, so those statics were still null and the whole server failed\nto start with a `NullReferenceException` wrapped in a\n`StartupFailedException`.\n\n- Dropped the three row counts from the log line, leaving the provider and\n  resolver counts\n\n`fix` (`airing`): **Stop date range reads throwing at the `DateOnly` bounds**:\n\n`GET /api/v3/AiringSchedule/Airing` returned an unhandled 500 for ranges near\neither end of the `DateOnly` domain, which are perfectly valid values that pass\nmodel binding and the `end < start` check.\n\n- `endDate ?? start.AddDays(7)` overflowed when the start was within a week of\n  `DateOnly.MaxValue`; both call sites now go through an `AddDaysClamped` helper\n- `EnumerateDays` stepped past `DateOnly.MaxValue` when a range ended on it, and\n  now walks day numbers instead\n\n`fix` (`api`): **Serialize airing timestamps as UTC and languages as names**:\n\n`AiredAt` meant two different things depending on where the row came from:\nestimated airings serialized with a `Z`, stored ones without, because\nNHibernate hands back `DateTimeKind.Unspecified` while the estimator produces\n`Utc`. `CreatedAt`/`LastUpdatedAt` were worse — `ToUniversalTime()` on an\n`Unspecified` value treats it as local, shifting the timestamp by the machine's\nUTC offset.\n\n- Added `AiringDateTimeExtensions.ToUtc`, which relabels `Unspecified` as the\n  UTC it already is rather than converting it, and routed every airing,\n  schedule and channel timestamp through it\n- `AiringTrack.Language` carried no `StringEnumConverter` and went over the wire\n  as an integer while its sibling `Kind` emitted a name\n\n`perf` (`airing`): **Filter before resolving and memoise per-read lookups**:\n\nA range read resolved every episode in the window through the full per-episode\npipeline before `MatchesFilters` threw the result away, and re-derived the same\nvalues for each one.\n\n- The day-bucket scan now applies the schedule-level filters up front, memoised\n  per schedule\n- `IsInSeason` reads a cached episode key set instead of rescanning\n  `ISeason.Episodes`, which was recomputed on every access\n- `GetAnidbAirDate` and `GetProvider` are memoised on the read context like the\n  lookups beside them; `GetProvider` was returning a fresh copy of the provider\n  info, and its `HashSet`, on every call\n- Settings are loaded once per read rather than per episode, so preferences can\n  no longer change midway through one\n- Channel de-duplication groups by the shared-track rule instead of an exact\n  stringified track set, so `[Subtitled en]` and `[Original ja, Subtitled en]`\n  collapse as they should\n- The range widening is clamped, so a read at the edge of the `DateTime` domain\n  no longer throws\n\n`fix` (`airing`): **Correct link sets, shared state and trailing estimate shifts**:\n\nFallout from an adversarial review of the airing schedule service.\n\n- `SetAirings` dissolved a link set of three or more when the head row was\n  deleted, unlinking the survivors individually instead of letting\n  `NormalizeLinkSets` promote the next smallest member as `RemoveAiring` does\n- `LinkAirings` counted its raw argument list, so the same airing passed twice\n  produced a self-pointing set of one instead of throwing\n- `_airingIDs` was a plain `Dictionary` mutated on queue-worker threads while\n  request threads read it, unlike every other shared map on the class\n- `_refreshWaiters` never dropped a key, growing one entry per provider and\n  entity for the lifetime of the process\n- `EstimateAiring` applied `TrailingShiftDays` to every AniDB-anchored estimate,\n  shifting gap-fills inside the aired range by a slip that happened later;\n  the profile now carries the boundary the shift starts from\n- `RefreshAiringScheduleJob` keyed itself on provider and entity id alone,\n  because `JobKeyBuilder` rejects enum members unless they are annotated, so a\n  refresh for series 5 collided with one for episode 5. It also lacked\n  `[DatabaseRequired]` while reading and writing repositories throughout\n- A raw `U+0000` byte sat in a string literal in `AiringScheduleUtility`, which\n  made the whole file read as binary to grep, diff and friends\n- Ten members of `IAiringScheduleService` documented an `InvalidOperationException`\n  they never throw, and two throws went undocumented\n\n`fix` (`airing`): **Log why a provider's airings were rejected**:\n\n`AiringScheduleValidationException` carries a reason per rejected airing, but\nthe refresh catch logged only its message — \"One or more airings were\nrejected.\" — which says nothing about which airing failed or why, leaving no\nway to tell a provider bug from bad upstream data.\n\n`feat` (`api`): **Report how many videos an airing's episode has**:\n\nA calendar row could tell whether the series was in the collection, but not\nwhether the episode itself was already held. `VideoCount` is on the default\nitem rather than behind `include`, because it resolves through the cached\nvideo repository's AniDB episode index rather than a lookup worth deferring.\n\n`fix` (`tmdb`): **Stop listing the same video twice for an episode**:\n\n`TMDB_Episode`'s `IEpisode.Videos` walked every AniDB cross-reference and\nconcatenated their files without de-duplicating, so a file covering two AniDB\nepisodes that both map to one TMDB episode came back once per cross-reference.\n`AniDB_Episode`, `AnimeEpisode` and `Anilist_Episode` all de-duplicate already.\n\nA copy of a production database has 37 TMDB episode and file pairs that hit\nthis.\n\n`chore`: **Sort usings and drop unused imports**:\n\nRan `scripts/sort_usings.py` over the tree, which reordered the `Airing` and `AniDB` repository namespaces and removed two imports nothing used any more.\n\n`feat` (`airing`): **Fire an event when an episode airs**:\n\nAdded `IAiringScheduleService.EpisodeAired`, raised by a background ticker\nthat keeps the next hour of airings in memory, so plugins, core and SignalR\nclients can react to an episode airing in real time instead of polling\n`GetAiringsInRange`.\n\n- `EpisodeAiredEventArgs` carries the enriched airing and the slot that\n  passed. It is raised once per **airing**, not per episode: an episode on\n  three channels raises it three times, and a consumer that wants \"this\n  episode aired\" de-duplicates by episode itself\n- Estimates are raised too, flagged by `IEpisodeAiring.IsEstimated`. An\n  estimated event is a prediction, not a fact, and there is no retraction\n  event if the estimate later moves; the real airing that follows has its own\n  stable ID and raises its own event\n- `EpisodeAiringNotificationService` is an `IHostedService` rather than a\n  recurring job, because the event has to land within a minute of the slot and\n  a queued job waits behind the worker pool and its acquisition filters. It\n  wakes every 30 s and works on wall-clock minute boundaries, so it never\n  drifts from the minute and never fires early\n- The horizon is one hour of airings, rebuilt every 15 minutes and immediately\n  on `ScheduleUpdated`, `AiringsUpdated` or `ProvidersUpdated`, so a delay\n  reported mid-horizon can't fire at the old time. Estimates are computed\n  rather than stored, so filling it runs the read path — which is why it stays\n  short\n- The \"fired through\" watermark is a `ScheduledUpdate` row\n  (`EpisodeAiringNotifications`). On boot it is moved forward to now and the\n  gap is logged rather than replayed: an hours-old prediction is worse than\n  none\n- Bridged to SignalR as `airing:episode.aired` through a new\n  `AiringEventEmitter`, one emitter per service as the rest of the aggregate\n  hub does, carrying the same `EpisodeAiring` shape the airings endpoint\n  returns\n- Documented in the plugin-facing `README.md` and in `AiringSchedule.md`, and\n  covered by five tests over the existing airing harness\n\n`refactor` (`airing`): **Replace `EpisodeAired` with a filtered subscription**:\n\nDropped the `IAiringScheduleService.EpisodeAired` event for\n`SubscribeToAirings(handler, options)`, which returns an `IDisposable`\nhandle. An event could do neither of the two things this needs: carry\nper-subscriber filtering, and let the producer do less work.\n\n- Each subscriber brings its own `EpisodeAiringFilteringOptions`, applied\n  to its own dispatch. `null` means everything\n- `EpisodeAiringNotificationService` builds its horizon from the union of\n  the live subscriptions, so `IncludeEstimates` — the expensive flag,\n  since estimates run through the read path rather than being stored — is\n  only set when a live subscriber wants them\n- With nobody subscribed there is no horizon, no estimate pipeline and no\n  dispatch at all; only the watermark walks forward. A subscription\n  version bump re-arms the horizon on the next tick rather than at the\n  next quarter-hour refresh\n- Subscriptions live in a `ConcurrentDictionary`; dispose is idempotent\n  and thread-safe, and each handler is wrapped so one bad plugin costs\n  itself its dispatch and nothing else\n\n`EpisodeAiredEventArgs` now carries the minute and an\n`IReadOnlyList<IEpisodeAiring>` instead of one airing, so a simulcast\nthat puts Beyblade X on テレビ愛知 and テレビ東京 at 11:25 is one dispatch\nrather than two. `EpisodeAiredSignalRModel` batches to match. This\ndissolves the old \"one event per airing, not per episode\" caveat: a\nconsumer groups the list by episode, channel or link as it likes.\n\nAdded `AiringEntityAnchor` (`Auto`/`Raw`/`Shoko`) to both\n`EpisodeAiringFilteringOptions` and `AiringScheduleFilteringOptions`, and\nsurfaced it as `entityAnchor` on `AiringScheduleController`. `Shoko`\nkeeps only what resolves to a shoko entity; `Raw` answers what the\nprovider stored. `Auto` infers from the entity a read was given and falls\nback to `Raw` where there is none, so it composes with\n`LinkedEntityAirings` rather than overriding it and existing callers are\nunchanged.\n\nLifted `DisposableAction` out of `KeyedEntityLockHelper` into its own\nfile in `Shoko.Server.Utilities`, made it idempotent, and used it for the\nsubscription handle. Only `IDisposable` crosses into the abstractions.\n\nAlso corrected the provider and resolver registration advice in the\nairing README: `GetExports<T>()` resolves the concrete type, so most\nproviders need no DI registration at all, a singleton registration is\nonly for plugins whose own code resolves the provider, and registering\nunder the interface pollutes the container for other plugins, splits\nsingleton state and breaks the `AddParts` identity check.\n\n`docs` (`airing`): **Correct how a provider identifies itself when writing**:\n\nThe write flow section told plugin authors to keep \"the\n`IAiringScheduleProvider` reference your constructor was given\", which reads as\nthough the interface were the concrete type and as though something hands a\nprovider its own reference. Nothing does: a provider passes `this`.\n\nIt also left out that the service checks by reference, so a second writer in the\nsame plugin only works when the provider is registered as a concrete singleton,\nwhich is the one case where that registration is needed.\n\n`docs` (`abstractions`): **Fix plugin DI advice in the release and streaming readmes**:\n\nBoth readmes told plugin authors to register their implementation under the\ninterface, which is wrong in every case. `PluginManager.GetExports<T>` finds\nimplementing types by reflection and asks the container for the *concrete*\ntype, so an interface registration is never read by the core, constructs a\nsecond instance, and leaves the container with a single-`T` resolution that\nwhichever plugin loaded last silently wins.\n\n- Documented the rule as three branches for `IReleaseInfoProvider`,\n  `IVideoStreamTransform` and `IPlaybackObserver`: no registration by default,\n  a concrete-type singleton when your own code resolves it, and never under the\n  interface\n- Replaced the `IReleaseInfoProvider` lifecycle hook table, which still listed\n  `PrepareForSave`, `OnReleaseSaved`, `OnReleaseCleared` and `OnSearchCompleted`\n  after they were removed, with the surviving `GetRescanDelay` and a pointer to\n  the `IVideoReleaseService` events\n- Corrected `FinalizeReleaseSearchJob` to fire `IVideoReleaseService.SearchCompleted`\n- Noted that a result with no cross-references counts as no match, that\n  `PreventRescan` blocks rescans outright, and that `GetRescanDelay` is only\n  consulted for providers that took part in the previous attempt\n- Corrected the streaming readme: the built-in `ScrobbleObserver` ships enabled,\n  as does any core-plugin observer, while transforms and plugin observers are\n  opt-in\n- Cut the em dashes, 9 and 14 respectively, down to none\n\n`docs` (`abstractions`): **Document the metadata-side extension contracts**:\n\nAdded plugin-facing readmes for the three metadata extension points that\nhad none.\n\n- `IImageCrossReferenceResolver`: the `(EntitySource, EntityType, EntityID)`\n  keying, how `ImageManager` consults resolvers only after every built-in\n  case misses, and what `ImageFilteringOptions.LinkedEntityImages` decides\n  when left `null`\n- `IResourceResolver`: how `GatherResourcesForEntity` appends to an entity's\n  own `Resource` list, with the AniDB `\"syoboi\"` cross-reference as the\n  worked example of a resource another plugin parses\n- `ISupplementaryMetadataProvider`: the three call sites, the\n  `SkipSupplementaryUpdate` gate, and why a new anime produces both an\n  `isNew: true` and an `isNew: false` call\n\n`docs` (`abstractions`): **Document the hashing, relocation and ignore rule contracts**:\n\nAdded plugin-facing readmes for the three video-side extension points that\nhad none.\n\n- `IHashProvider`: where hashing sits in the import pipeline, how\n  `AvailableHashTypes` differs from the user-owned\n  `HashProviderInfo.EnabledHashTypes`, and what `AddParts` seeds by default\n- `IRelocationProvider`: the preset model, the drop-source and\n  drop-destination flags on `ShokoManagedFolder`, and which of the four\n  support flags the service actually gates on\n- `IManagedFolderIgnoreRule`: when a scan consults it, that `true` means\n  ignore, and that the file watcher does not consult it at all\n\n`docs` (`abstractions`): **Add a plugin-authoring overview**:\n\nAdded `Shoko.Abstractions/README.md`, covering the parts of plugin\nauthoring that every plugin has to get right regardless of which\ncontract it implements.\n\n- Documented that `IPlugin` needs a public parameterless constructor,\n  because `PluginManager` builds the type twice and the first build,\n  during discovery in a throwaway load context, uses\n  `Activator.CreateInstance` before any container exists. A plugin whose\n  only constructor takes dependencies is dropped during discovery and\n  never reaches the injecting `ActivatorUtilities.CreateInstance` path\n- Documented where dependencies belong instead:\n  `IPluginServiceRegistration`, `IPluginApplicationRegistration`, and an\n  `IHostedService` for anything needing DI plus lifecycle, pointing at\n  `EpisodeAiringNotificationService` as the worked example\n- Stated the three-branch DI registration rule once, listing the eleven\n  `GetExports<T>`-discovered contracts it applies to\n- Covered identity metadata (`Shoko.BuildTools`), configuration\n  discovery and `ConfigurationProvider<T>`, queue job and controller\n  auto-registration, plugin directories, and the load-time gates\n- Trimmed the duplicated registration sections out of the airing,\n  release and streaming READMEs, leaving each with only what is specific\n  to its own contract and a link to the overview\n- Rewrote the airing subscription example as a `BackgroundService`; it\n  previously put constructor dependencies on the `IPlugin` class, which\n  would have failed discovery\n\n`docs` (`abstractions`): **Document the anidb, anilist and tmdb services**:\n\nAdded a README next to each metadata source's service folder, covering\nwhat a plugin resolves from DI and how to use it without upsetting the\nupstream.\n\n- Framed all nine interfaces as consumed services rather than extension\n  points, since none are discovered by reflection\n- Pointed the read path at the local cache first (`IShokoSeries.AnidbAnime`,\n  `.TmdbShows`, `.AnilistAnime`, and `IAnidbAnime.Resources` for a source's\n  own ids)\n- Spelled out the rate limits and etiquette per source: AniDB's UDP and\n  HTTP budgets, bans and `AnidbHttpBannedException`, AniList's quota\n  headers and first-error breaker, TMDB's sliding window and 5XX breaker\n- Called out that linking never fetches, so `AddShowLink` and\n  `AddAnimeLink` need a `ScheduleUpdateOf...` after them\n\n`docs` (`abstractions`): **Document the video and web service surfaces**:\n\nAdded plugin-facing READMEs for `Shoko.Abstractions/Video/Services` and `Shoko.Abstractions/Web/Services`. Both folders hold services a plugin consumes rather than contracts it implements, so the pages cover how to resolve each service, what its methods do, and the traps worth knowing, and point at the sibling folders for the extension points the services drive.\n\n- Called out `AddParts` as core-only, with a table mapping each service to the contract it collects\n- Left the removed parallel mode out of `IVideoReleaseService` and noted that its XML docs still describe it\n\n`docs` (`abstractions`): **Add plugin-facing readmes for five core services**:\n\nDocumented `ISystemService`, `IConfigurationService`,\n`IConnectivityService`, `ILogService` and `IActionService` for plugin\nauthors, covering what each is for, how a plugin gets hold of it, and\nwhat to watch out for.\n\n- **Core** covers the startup timeline and which event to initialize\n  against (`AboutToStart` runs synchronously, so a throwing handler\n  fails startup), the shutdown and database-blocked gates, and why\n  `ISystemService.StaticServices` is a last resort behind DI and\n  `ServerAboutToStartEventArgs.ServiceProvider`\n- **Config** covers `ConfigurationProvider<T>`,\n  `INewtonsoftJsonConfiguration`, the marker interfaces, storage via\n  `[StorageLocation]` and the WebUI attributes, plus the `[Required]`\n  trap that made a plugin fail to load while its token was unset\n- **Connectivity** is deliberately short: `[NetworkRequired]` already\n  gates queue jobs on `PartialInternet`, so most plugins never call\n  `IConnectivityService` themselves\n- **Logging** separates getting an `ILogger<T>` from reading log files\n  back through `ILogService`, and frames log volume as a question of\n  level rather than of frequency\n- **Actions** is both sides at once: implementing `IExecutableAction`\n  and the four scoped base classes, and invoking through\n  `IActionService`\n\nNoted that `IConfiguration` and `IExecutableAction` are discovered via\n`GetTypes<T>()` rather than `GetExports<T>()`, so the three-branch\nregistration rule does not apply to either, and linked the main readme\nfor the ones where it does.\n\n`docs` (`abstractions`): **Add filtering, user and plugin namespace readmes**:\n\nDocumented three plugin-facing areas that had no readme, each written\nfrom the interfaces as they stand rather than from the folder layout.\n\n- `Filtering/Services` covers the four core-provided services, the\n  `IFilterableInfo`/`IFilterableUserInfo` projection an expression\n  evaluates against, building an ad-hoc filter with `GenericFilter`,\n  and why `InSeasonExpression` matches every season a series aired in\n- Called out that `CreatePreset`/`UpdatePreset` reject an expression\n  type outside `Shoko.Abstractions`, so a plugin's own\n  `FilterExpression` cannot be persisted in a preset\n- `User/Services` covers the four user data levels, the 95% rule\n  behind video-to-episode propagation, `updateStatsNow` batching, the\n  import source loop guard, and `IAuthenticationThrottleService` call\n  order\n- `Plugin` documents the types in the namespace itself and links to\n  the plugin overview for the authoring narrative\n\n`refactor` (`streaming`): **Rename `ScrobbleObserver` to `LegacyScrobbleObserver`**:\n\nThe observer only exists to honour the `streamPositionScrobbling` query\nparameter for players that predate the dedicated `/Scrobble` endpoint, so the\nname now says as much. Its display name is `Legacy Scrobbler` to match.\n\nRenaming changes the part ID, which `VideoStreamPipelineService.GetPartID`\nderives from the type's full name, so anyone who had toggled the observer falls\nback to the default. It defaults to enabled for core plugin parts and the code\nis new enough that nothing has shipped with the old ID.\n\n`docs` (`streaming`): **Describe what an `IPlaybackObserver` actually observes**:\n\nAn observer is called when bytes are served, so what it sees is the sequence of\ncache misses, in whatever order the player needed them. The readme presented\nthe HLS position as precise and the worked example suggested deriving a watched\npercentage from it.\n\n- A player requests segments ahead of what it shows, and may request segments it\n  never shows\n- A seek into cached content produces no request at all, while a seek into\n  uncached content does and can be for an earlier point than the last, so the\n  reported position jumps both ways\n- Progressive playback is weaker again, inferring the position from a byte range\n  reaching the end of the file, which does not confirm the bytes reached the\n  player\n\nPoints at the `/Scrobble` endpoint as the way to get a position the client\nvouches for, and drops the same \"playback reaches the end\" phrasing from the\nuser data readme.\n\n`docs` (`abstractions`): **Index every contract from the plugin overview**:\n\nThe overview listed only the three READMEs that existed when it was written, so\nhash and relocation providers were absent from the one page a plugin author\nstarts on. Relocation is called out as the oldest of the contracts, since the\n4.x plugin system was built around renaming and moving files and most plugins\npredating 5.x are relocation providers.\n\nSplit into contracts a plugin implements and services it consumes, which is the\ndistinction the per-contract READMEs already draw.\n\n`docs` (`abstractions`): **Describe each contract in the overview index**:\n\nOnly relocation carried a description, which read as though it were the\nimportant one rather than the first of a list. Each entry now says what the\ncontract is for, taken from the opening of the page it links to.\n\n`docs` (`abstractions`): **Give the overview index its own headed sections**:\n\nThe index was three bold labels with several pages crammed onto one bullet,\nwhich made it hard to scan for the page you wanted. It is now a `Where to go\nnext` section with a heading per audience, and one page per bullet.\n\n`docs` (`abstractions`): **Cover the whole metadata services folder**:\n\nThe page opened by admitting it documented one of the six interfaces in\nthe folder. Restructured it around the folder, with the implement/consume\nsplit the sibling pages use, and kept every finding from the\n`ISupplementaryMetadataProvider` section intact.\n\n- Documented `IMetadataService`: the shoko- and provider-side lookups,\n  the three-value `ProviderName` and the AniList gap, the twelve relayed\n  events, custom tags, and `GatherResourcesForEntity` pointing at\n  `Metadata/Resources/README.md`\n- Documented `IShokoGroupManager`: nesting, `Series`/`AllSeries` versus\n  `Groups`/`AllGroups`, `MainSeries` resolution, the `GroupUpdateData`\n  set-flag pattern, auto versus manual naming, `DeleteGroup` cascading\n  through empty-group cleanup, and the auto-grouping settings\n- Documented the consumer side of `IImageManager`: `GetImagesForEntity`\n  and the `IWithImages` defaults, `ImageFilteringOptions` including the\n  `LinkedEntityImages` tri-state and its per-entity default,\n  `AddImageCrossReference` and preferred-image handling, and the\n  download and purge calls\n- Recorded four places where the group doc comments and the code\n  disagree, including `GroupData.ParentGroup` being dropped by\n  `CreateGroup` and `AllowDissimilarTitleExclusion` reading backwards\n- Listed the page under \"Server services a plugin calls\" in the\n  abstractions README\n\n`docs` (`streaming`): **Correct the legacy scrobbler's own summary**:\n\nIt claimed to be disabled by default, but `AddObserverParts` sets\n`defaultsToEnabled` for core plugin parts, so it ships enabled. It also\ndescribed marking a video watched \"once playback reaches the end\", the same\nfetch-for-viewing conflation already corrected in the streaming readme.\n\n`docs` (`abstractions`): **Correct plugin-facing errors in the service readmes**:\n\nFixed errors across the plugin-facing READMEs that would have led an author\ninto code that does not compile, or that silently destroys data.\n\n- Rewrote the examples that put constructor dependencies on `IPlugin`, in\n  `Core/Services`, `Video/Services` and `Web/Services`, as hosted services or\n  DI-registered classes, matching the shape `User/Services` and\n  `Metadata/Airing` already use. Dropped the two `IPlugin.Load()` references;\n  no such member exists and nothing calls one\n- Corrected the `ForAniDB` call sites to invoke it on the type, and explained\n  that a `static` member of a C# 14 `extension` block is not an instance\n  extension method\n- Corrected the `purge: true` claim on the TMDB and AniList pages: neither\n  `RemoveShowLink` nor `RemoveAnimeLink` checks for remaining links, so a\n  purge while multi-linked destroys another anime's cached entity\n- Fixed the compile errors in `Metadata/Airing`: `Key` is a `string` against an\n  `int` `IEpisode.ID`, `IEpisodeAiring.EpisodeID` is a `string`, `DataSource`\n  is a closed enum whose plugin member is `DataSource.Plugin`, and the worked\n  example used an undeclared `client`\n- `User/Services`: added the `Name` that `IPlaybackObserver` requires, narrowed\n  the `params string[]` claim to `Add`/`Remove`, and documented what\n  `DeleteUser` really throws\n- `Filtering/Services`: core expressions take the abstract\n  `FilterExpression<bool>` and cannot mix with a sealed\n  `GenericFilterExpression`; `GetAllFilteredGroups` returns\n  `IReadOnlyList<IShokoGroup>`\n- `Metadata/Anidb`: a `MylistSyncPlan` is a snapshot carrying values, so an old\n  plan is not safe to replay; `PurgeAllUnusedAnime()` schedules per-ID jobs\n- Qualified `ProviderName` as `IMetadataService.ProviderName` where it is\n  nested, corrected the `ReadRange` ordering default, and named the real\n  entry point that throws on a missing ED2K\n- `Actions/Services`: queue concurrency attributes are read only off\n  `IQueueJob` types, so they do nothing on an action and every action shares\n  one unconstrained pool; `Execute`'s token is always `CancellationToken.None`\n- `Config/Services`: an `IBaseConfiguration` throws nothing, it returns a fresh\n  instance on load and `false` on save\n- Documented the provider-rename hazard on `Video/Release`, as the hashing and\n  relocation pages already do, and resolved the schedule-identity and\n  `IFilterableUserInfo` self-contradictions\n\n`feat` (`queue`): **Flow the worker's token to jobs via `IJobCancellationAccessor`**:\n\n`ActionExecutionJob` called `action.Execute()` with no argument, so every\n`IExecutableAction` ran with `CancellationToken.None` while both the inline\ncomment and the XML on `IExecutableAction.Execute` claimed the job's own\nlifetime token was passed. No token existed to pass: `IQueueJob.Process()`\ntakes no parameters.\n\n- Added `IJobCancellationAccessor` / `JobCancellationAccessor`, a scoped\n  accessor registered next to `JobChainContextAccessor`\n- `Worker` stamps its pool's shutdown token onto the job scope per job,\n  before `Process()`, on the same path as `SetCurrentJob`\n- `ActionExecutionJob` injects the accessor and passes the token to\n  `action.Execute(...)`\n- Corrected the comment and the XML to say what the token actually is: the\n  worker pool's shutdown token, cancelled on server shutdown or a queue\n  stop and at no other time, never for a single job\n\n`docs` (`actions`): **Follow the token now that the worker supplies one**:\n\n`Execute`'s token was `CancellationToken.None` until `IJobCancellationAccessor`\nlanded. It is now the pool's token, so the practical advice survives, an action\nstill cannot be cancelled on its own, but the absolute claim does not.\n\n`fix` (`images`): **Accept valid template URLs again**:\n\nThe scheme check in `ImageManager.SetTemplateUrlForSource` and\n`ImageTemplateUrlConfiguration.Validate` joined the two comparisons with\n`||`, so `uri.Scheme != Http || uri.Scheme != Https` was true for every\npossible scheme — no scheme can be both at once. Every non-null template\nURL was rejected, including valid `http://` and `https://` ones, which\nbroke `PUT /api/v3/Image/Source/{source}` outright and made the\nconfiguration validator reject every template URL it was handed.\n\n- Joined both scheme comparisons with `&&` so only a URL that is neither\n  http nor https is rejected\n- Dropped the unused `urlErrors` local in `SetTemplateUrlForSource`, left\n  over from the validator this predicate was copied from\n- Added `ImageTemplateUrlConfigurationTests` covering valid http/https\n  template URLs, non-http schemes, relative and malformed URLs, and a\n  missing `{0}` placeholder\n\n`feat` (`airing`): **Add `MergeAirings` for delta writes to a schedule**:\n\nAdded a second write for providers that only ever see part of a run at a\ntime. `SetAirings` reads its submission as the schedule's whole line, so\nan airing left out of it is removed; `MergeAirings` takes the removals as\nan explicit argument and leaves everything it was not handed alone.\n\n- Split `AiringScheduleUtility.InferAirings` into `PrepareLine`, `Infer`\n  and the two public entry points, so the whole-line write derives its\n  removal set by diff and the delta write states it, and both reach the\n  same hiatus and cause detection\n- Carried an untouched airing through `Infer` as a submission of its own\n  stored state, so it counts as part of the line without being rewritten,\n  and dropped it from the result when the write left it as it was\n- Extracted the service's write into `WriteAirings`, shared by\n  `SetAirings` and `MergeAirings`, so validation, link re-pointing and\n  the `AiringsUpdated` event are the same on both\n- Checked a removal the way every other change is checked: it has to be a\n  stored airing, owned by the provider, on the schedule being written to,\n  and not also submitted by the same call\n- Gave `EpisodeAiringUpdateOptions` `IsFinished`, `FirstEpisodeNumber` and\n  `LastEpisodeNumber` with the usual `Has…Set` flags, so a write can state\n  the coverage it judges its own removals against. It is never written\n  back to the schedule and never relaxes validation\n- Documented the write in `Shoko.Abstractions/Metadata/Airing/README.md`\n  and took the em dashes out of that file\n\n`docs` (`abstractions`): **Close three gaps the first consumer hit**:\n\nFeedback from porting the MCP plugin against these pages.\n\n- `IActionService` was reachable only from the implement side of the index,\n  although its own page opens by saying the folder is both sides at once, so a\n  reader looking for services to call skipped the folder entirely\n- Nothing said how to reference `Shoko.Abstractions` without shipping a copy of\n  it, even though the load-failure list warns about the reference and shipping\n  a copy is what stopped a plugin loading\n- `Shoko.BuildTools.Targets` is published to nuget.org rather than the local\n  folder feed, so following the identity metadata advice failed to restore\n\n`fix` (`airing`): **Stop de-duplication collapsing separate airings**:\n\n`DeduplicateChannel` grouped a read's airings by `ChannelID` alone and\ndropped every view `IsSameRelease` matched, and `IsSameRelease` compares\nonly track sets. Since an airing's tracks are its schedule's, every\nairing of one schedule looked like a copy of the first, so\n`GetAiringsForSchedule` answered with one airing however many episodes\nthe schedule covered, and a channel's own repeat broadcasts of an\nepisode disappeared from every read.\n\n- Keyed de-duplication on the episode as well as the channel, and\n  collapsed whole schedules against each other rather than single\n  airings, so two providers reporting one slot still become one while a\n  schedule's own second slot for an episode survives as the rerun it is\n- Moved the `PreferredOnly` reduction from \"one airing overall\" to \"one\n  airing per episode\", and made `GetAiringsInRange` narrow to its window\n  before it reduces, so an episode is answered with its best airing in\n  the window rather than dropping out of the day it airs on\n- Resolved estimates through `GetAiringByID`, which only ever resolved\n  stored rows although an estimate's ID is derived the same way and is\n  handed to clients as an addressable ID\n- Brought `/api/v3/AiringSchedule/Channel/{channelID}/Airing` in line\n  with `/api/v3/AiringSchedule/Airing`: it now takes `type`,\n  `includeMissing`, `includeRestricted`, `preferredOnly` and\n  `includeDelayedOriginalSlots`, and hides restricted (H) series by\n  default as its sibling does. `kind` still defaults to every kind\n  rather than to `Original`, because a streaming channel carries no\n  `Original` track at all\n\n`docs` (`api`): **Correct the airing `language` filter examples**:\n\n`AiringSchedule.md` gave `en`, `eng`, `pt-BR` and `pt-PT` as examples for\n`language`, none of which bind: the parameter is a `TitleLanguage`, so a\nlanguage code is dropped by the comma-delimited binder and the filter\nsilently becomes no filter at all. Documented the enum names a caller\nshould pass, and kept the note about how the matching itself works,\nwhich is per language rather than per code.\n\n`docs` (`api`): **Drop the em dashes from the airing schedule guide**:\n\nReplaced all 24 em dashes in `AiringSchedule.md` with a colon, a semicolon,\na comma or parentheses, so it matches the 23 READMEs under\n`Shoko.Abstractions`, which are at zero. The wording and the structure are\notherwise untouched.\n\n`refactor` (`airing`): **Split the write event into added, updated and withdrawn**:\n\nA write raised one `EpisodeAiringsUpdatedEventArgs` with\n`UpdateReason.Updated` carrying every surviving airing, and raised nothing at\nall for the rows it deleted, so a consumer could tell neither what was new\nnor what had vanished.\n\n- Split the payload into `Added`, `Updated` and `Withdrawn`, leaving\n  `Airings` as the three of them in that order\n- Called the third one `Withdrawn` because a removal is not always a delete:\n  a slot still ahead of us is kept as a slotless hiatus row, and only a slot\n  already past is deleted as history\n- Kept `Reason` as the one coarse value: `Added`, `Removed` or `None` when\n  the write only did that one thing, and `Updated` for everything else\n- Carried a hiatus through the inference on `InferredAiring.IsWithdrawn`, so\n  a row kept without a slot is reported as a withdrawal rather than as an\n  update\n- Gave `SetAirings` the skip-if-unchanged treatment `MergeAirings` already\n  had, weighing the whole row rather than only what the inference carries, so\n  a write that changes nothing moves no `LastUpdatedAt` and reports nothing\n- Filled the event the same way from both entry points, so a consumer cannot\n  tell which one a provider used\n\n`feat` (`airing`): **Drive provider sweeps from core**:\n\nAdded `ISweepingAiringScheduleProvider`, an opt-in sub-interface a provider\nimplements alongside `IAiringScheduleProvider` to have the server walk its\nwhole source for it. Core owns enablement, scheduling, pacing and bookkeeping;\nthe provider owns only the unit of the walk, which genuinely differs per\nsource.\n\n- `SweepAsync(string?, CancellationToken)` takes the last cursor and answers\n  the next one. The cursor is opaque to core, stored capped at 512 characters,\n  and `null` means the sweep is finished\n- The token is a deadline source linked to the worker's own, so the provider\n  observes exactly one token for both the chunk's budget and shutdown. The\n  budget is core's, from `AiringScheduleServiceSettings.SweepBudgetSeconds`,\n  since a chunk holds a shared queue worker while it runs\n- Classified five ways as `AiringScheduleSweepOutcome`: `Completed`,\n  `TimedOut`, `Stopped`, `Cancelled` and `Failed`, by asking which source was\n  cancelled, which is documented as best effort rather than exact\n- `SuggestedSweepInterval` is the provider's declaration and\n  `AiringScheduleProviderInfo.SweepInterval` the user's value, the way\n  `AvailableKinds` and `EnabledKinds` already pair, clamped to fifteen minutes\n  and settable over `/api/v3/AiringSchedule/Provider`\n- A chunk that hands back the cursor it was given, or times out without having\n  returned one, counts as no progress on `AiringScheduleSweepState`. Three in a\n  row holds the sweep back to the provider's interval instead of resuming at\n  the next tick, and the log distinguishes too slow from stuck\n- `AiringScheduleSweepState` is one row per provider carrying the cursor, the\n  last run, the last outcome and that count, with the three-backend schema and\n  a migration. No run history: each chunk raises `SweepCompleted` and logs a\n  line with the duration, bridged to SignalR as `airing:provider.swept`\n- `SweepAiringSchedulesJob` dispatches chunks for the providers that are due,\n  and `SweepAiringScheduleProviderJob` runs one chunk for one provider,\n  re-queued by the service while the walk keeps moving\n\n`docs` (`airing`): **Document core-driven sweeps**:\n\nCovered the sweep contract for plugin authors and the two provider fields and\nthe push message a client sees.\n\n- Added `Core-driven sweeps` to the airing README: what a chunk is, what core\n  owns around it, the one token and the five outcomes, the cursor contract\n  including no-progress detection, and that implementing the interface needs no\n  DI registration because core sweeps the instance it already holds\n- Said there that a sweep writes with `MergeAirings` rather than `SetAirings`,\n  since a chunk holds part of a line and not the whole of one\n- Split `RefreshAsync` from a sweep in the contract bullets, the first being on\n  demand for one entity and the second the walk of a whole source\n- Documented `IsSwept`, `SweepInterval` and `airing:provider.swept` in the v3\n  guide, including that no sweep history is kept server-side\n\n`feat` (`queue`): **Let a job declare its own watchdog threshold**:\n\nAdded `IJobWatchdogThreshold`, a DI-registered per-job-type threshold the\nwatchdog asks on every poll, so a job with a deadline longer than\n`WatchdogTimeoutSeconds` stays watched instead of reaching for\n`[LongRunning]`. Anything the threshold cannot answer with, including a\nthrow, falls back to the global one, and a type declaring both a\nthreshold and the attribute is watched against the threshold.\n\nRaised `SweepBudgetSeconds` to 60 and its ceiling to 600. A rate limited\nprovider gets more of its walk done per chunk and pays the per-chunk\noverhead half as often, and `SweepAiringScheduleProviderJob` is capped at\nfour at a time, so four chunks at the full budget is the worst case.\n\n- Split the threshold lookup out of `JobWatchdog` into\n  `JobWatchdogThresholds`, which also owns the `[LongRunning]` exemptions\n- Registered `AiringScheduleSweepWatchdogThreshold`, which is 90 seconds\n  until the budget passes a minute and `1.5 x` the budget after that\n- Documented that shutdown waits for the chunk in flight, so a provider\n  ignoring its token holds the server for the rest of its budget\n\n`docs` (`images`): **State that a plugin image source needs a template URL**:\n\nAn image source with no template URL makes `AddImage` throw\n`MissingImageSourceTemplateUrlException` for every image, and core seeds a\ntemplate URL for AniDB, TMDB and AniList only, so every other source has\nto register one with `SetTemplateUrlForSource` before adding its first\nimage. Nothing said so.\n\n- Added a section to the `IImageManager` guide covering the three seeded\n  sources, when to register a template URL and why the existing value is\n  left alone, that the resource ID is the remainder of the asset URL, and\n  that a resource ID over 128 characters is refused rather than truncated\n- Carried the same points into the doc comments on `AddImage`,\n  `SetTemplateUrlForSource` and `IImage.ResourceID`\n\n`refactor` (`airing`): **Resolve the sweep watchdog threshold without a cast**:\n\n`AiringScheduleSweepWatchdogThreshold` took `IAiringScheduleService` and cast it\nto `AiringScheduleService` to reach the internal `GetSweepBudget()`. Only the\ninterface was registered, so the cast was the only way in, and it would have\nthrown on every watchdog poll had that registration ever changed, leaving the\njob silently watched against the global threshold instead of its own.\n\n- Registered `AiringScheduleService` concretely and forwarded\n  `IAiringScheduleService` to it, so both resolve to one instance\n- Injected the concrete type into the threshold and dropped the cast\n\n`chore` (`queue`): **Bump `Shoko.QueueProcessor` to `6.0.0-alpha.7`**:\n\n`6.0.0-alpha.6` is published and is what `upstream/master` carries, so the\npublic surface added since it cannot ship under the same version.\n\n- `IJobCancellationAccessor` and `JobCancellationAccessor`, giving a job the\n  token of the pool running it\n- `IJobWatchdogThreshold` and `JobWatchdogThresholds`, letting a job declare\n  how long it may run before the watchdog reports it\n- `WorkerPoolManager` takes the declared thresholds, so its constructor gained\n  a parameter\n- `QueueProcessorOptions` and `LongRunningAttribute` documentation\n\n`fix` (`airing`): **Judge retention on the schedule a write leaves behind**:\n\nAiring retention was enforced in two places that disagreed. `RunRetentionSweep`\nremoves a schedule only when its *latest* airing has aged out, so a run that\nstill airs keeps its whole history, while the write path rejected every\nindividual airing older than the cutoff and failed the submission with it. A\nback-catalogue show with a recent episode was refused wholesale even though the\nsweep would never have touched it.\n\n`ValidateAirings` no longer looks at slots at all. `WriteAirings` runs the check\nonce, on the result of the delay inference, which is the schedule exactly as the\nwrite would leave it: the rows it stores, the untouched rows it leaves alone, and\na removal kept as a hiatus, which is still part of the line. `AddOrUpdateAiring`\nand `UpdateAiring` judge the stored schedule with the one changed row folded in.\nA write is refused only when nothing at all would be left inside the window, and\nthe rejection is reported under the key `#schedule` rather than an airing's,\nsince the whole schedule is what would go.\n\n- Matched the slotless arm to the sweep's: a line with no dated airing has no\n  date to judge and is kept\n- Ran the check before the row is touched on both single-airing paths, so a\n  refusal leaves the stored airing as it was\n- Documented the rule on `IAiringScheduleService` and in the airing README\n- Trimmed the AniList provider's own submission comments to what they do\n\n`fix` (`airing`): **Delete what a `MergeAirings` write names for removal**:\n\n`MergeAirings` and `RemoveAiring` disagreed about what naming an airing\nmeant. Both are explicit, but only `RemoveAiring` deleted: a named\nremoval reached the same inference loop a `SetAirings` omission does, so\na future-slotted airing came back as a hiatus and there was no way to\ndelete one through a delta write at all. `RemoveAiring`'s own doc\nalready asserted the opposite rule.\n\nThe rule is now that explicit removal deletes and absence is a hiatus.\nA named removal is deleted whichever side of now its slot is, matching\n`RemoveAiring`, and a provider whose removal really is its source\ndropping a slot asks for the old judgement with the new\n`EpisodeAiringUpdateOptions.KeepRemovalsAsHiatus`.\n\n- Split the removal judgement out of `Infer` into `SplitRemovals`, which\n  both the inferring and the store-as-submitted paths run, so the new\n  option governs a named removal with `InferDelays` on or off\n- Left a `SetAirings` omission alone: it is still a hiatus, and\n  `InferDelays = false` still deletes it, since that flag says what a\n  literal submission means rather than what a removal does\n- Stated the rule on `SetAirings`, `MergeAirings` and `RemoveAiring`,\n  and in the airing README\n- Added tests for both settings, including the retention interaction: a\n  removal kept as a hiatus holds on to the slot it lost and still counts\n  towards the window, while a deleted one stops counting\n\n`refactor` (`airing`): **Drop the single-airing writes from the airing contract**:\n\n`IAiringScheduleService` carried `AddOrUpdateAiring`, `UpdateAiring` and\n`RemoveAiring` alongside the batch writes, with no caller anywhere: every\nprovider that exists uses `SetAirings` or `MergeAirings`, and nothing in\n`Shoko.Server` or the REST surface reached for them. They were also\ndivergent, since `AddOrUpdateAiring` hand-rolled its own move detection\nagainst `AiringInferenceOptions.DefaultMoveThreshold`, never inferred\n`IsDelayed` and ran no cause detection, so the same airing written through\nit and through `MergeAirings` could land as different stored state.\n\nEvery single-airing operation is a one-element `MergeAirings` call, which\nalso gets the inference, so the three are gone rather than kept.\n\n- Removed the three members from the interface and from\n  `AiringScheduleService.Airings`\n- Removed `EpisodeAiringUpdateData`, the single-airing `ValidateRetention`\n  overload and `HasMoved`, which only those paths used\n- Kept the batch `ValidateRetention`, `GetOwnedAiring` and `ValidateAirings`,\n  which `SetAirings` and `MergeAirings` still need\n- Ported the link-head promotion and the retention-on-a-move cases onto\n  `MergeAirings`, and dropped the two that `MergeAirings` and `SetAirings`\n  already pinned\n- Reworked the `README.md` prose and the `<see cref>` links that named them\n\n`feat` (`abstractions`): **Mask configuration secrets on the way out and restore them on the way back**:\n\nConfiguration objects carry live credentials and every serializer that\ntouched them wrote them out in plaintext, so `AniDb.Password`,\n`AniDb.AVDumpKey`, `Database.Password` and `TMDB.UserApiKey` reached the\nv3 configuration surface and any plugin that serializes a configuration\nfor a client of its own. `PasswordPropertyTextAttribute` is a\n`System.ComponentModel` marker that Newtonsoft ignores entirely, so\nnothing was acting on it outside `SettingsProvider.DumpSettings`.\n\nMasking is a property of the transport, not of the model. The model keeps\nthe truth, so `Load<TConfig>()` still hands a plugin the real\ncredentials; only serialization for something outside the server masks.\n\n- Added `ConfigurationSecrets` to `Shoko.Abstractions`, a document-level\n  masker and restorer keyed off `[PasswordPropertyText]`\n- Exposed it through `IConfigurationService` as `SerializeMasked`,\n  `MaskSecrets` and `RestoreMaskedSecrets`, so core and plugins share one\n  implementation rather than drifting apart\n- Made the write path a post-deserialize merge rather than a\n  `JsonConverter`, which cannot see the stored value: an absent property\n  or the sentinel keeps what is stored, any other value replaces it, and\n  an explicit `null` or `\"\"` clears it\n- Ran the restore inside `SaveInternal` and `Validate` before any\n  validation, so a required secret is never judged on a sentinel\n- Rejected the sentinel as a settable value, so nobody is locked out\n  without a diagnostic\n- Paired collection secrets by the `[Key]` property the schema generator\n  already treats as a record's identity, and by the dictionary key for\n  dictionaries; a list without one is refused rather than matched by\n  position\n- Masked the v3 configuration endpoints, `GET /api/v3/Settings`, the\n  relocation preset configuration endpoints and both sides of the\n  `ConfigurationActionResult` diff\n- Added a test over every configuration type that fails when a property\n  named like a credential is neither marked nor allowlisted\n"
    },
    {
      "version": "6.0.0.470",
      "tag": "v6.0.0-dev.470",
      "commit": "15cc66ad621fa61dff3840447842244c42331bab",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.470/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-16T13:57:32Z",
      "releaseNotes": "\n`fix` (`api`): **Apply settings patch validation and stop per-item json patch loss**:\n\nPer-item JSON Patch operations on settings lists mutated the list\ninstance returned by the property getter in place, so property setters\nnever ran and the changes were silently discarded or left derived state\nstale.\n\n- Made `TMDBSettings.InternalImageLanguageOrder` the single source of\n  truth with a normalizing setter and turned the enum `ImageLanguageOrder`\n  into a computed `[JsonIgnore]` view, so per-item ops on\n  `/TMDB/ImageLanguageOrder` mutate the stored list\n- Added `ImportSettings.ResetExcludeRegexes` and invalidate the compiled\n  exclude regex cache in `SettingsProvider.OnSettingsSaved`, since\n  per-item ops on `/Import/Exclude` bypass the cache-invalidating setter\n- Returned 400 via `ValidationProblem` from `SettingsController.SetSettings`\n  when `ApplyTo` errors leave `ModelState` invalid instead of saving and\n  returning 200\n- Added regression tests covering per-item ops on both lists, whole-array\n  replace normalization, and ModelState error surfacing\n"
    },
    {
      "version": "6.0.0.469",
      "tag": "v6.0.0-dev.469",
      "commit": "2fa9089a25b73896bc866eed46904f775e52aa96",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.469/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-15T22:36:56Z",
      "releaseNotes": "\n`fix` (`database`): **Guard the 182 primary-key alters against dropped tables**.\n"
    },
    {
      "version": "6.0.0.468",
      "tag": "v6.0.0-dev.468",
      "commit": "ebdac82e2310e9070a827840b20abc63b29a16ce",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.468/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-15T22:06:48Z",
      "releaseNotes": "\n`fix`: **Collect a shoko season's TMDB seasons from its own episodes**:\n\n`IShokoSeason.TmdbSeasons` and `IShokoSeason.LinkedSeasons` on `AnimeSeason`\nfiltered the series' episodes with `SeasonNumber != seasonNumber`. An episode's\nseason number follows its type, so the filter never matched and both always came\nback empty. They now match the season's own episodes, like the rest of the type.\n\n- Added `AnimeSeasonTmdbLinkTests` covering a normal and a specials season\n\n`fix` (`api`): **Serve the HLS manifest from its session path**:\n\n`Stream/Hls/master.m3u8` returned a manifest whose `init.mp4` and\n`segment-{index}.m4s` URIs resolved against `Stream/Hls/`, where no route\nexists, so no HLS transform could be played. It now mints the session and\nredirects to `Stream/Hls/{sessionID}/master.m3u8`, and `BuildManifest`\ncarries the request's query string onto every URI so an `apikey` in the\nquery reaches the segments too. `Stream/Direct` keeps the query string\nacross its redirect for the same reason.\n\n`feat` (`abstractions`): **Add `IHlsPresentationRendition`**:\n\nAdded an HLS rendition shape that serves its own presentation, for output\nthe core's single-variant, fixed-duration manifest cannot describe:\nalternate audio renditions, a bitrate ladder, or segments cut at source\nkeyframes.\n\n- `OpenResourceAsync` receives every request under\n  `Stream/Hls/{sessionID}/`, including `master.m3u8`, with the relative\n  path and the query string\n- `HlsResource` carries the content type, an optional `ETag`, and the\n  position observers are notified with\n- `IHlsStreamRendition` and its routes are unchanged\n\n`fix` (`api`): **Mark optional authentication on the stream pipeline routes**:\n\nThe `Stream/Transforms`, `Stream/Hls` and `Stream/Direct` routes let\nanonymous callers through only when `AllowAnonymousFileStreamingInAPIv3`\nis set, like `Stream` itself, but were missing `OptionalAuthentication`,\nso the OpenAPI document did not list the API key for them.\n\n`fix` (`streaming`): **Keep a stream session alive while a response is streaming**:\n\nA session's idle time only reset when a request arrived, so a progressive\nstream played over one long range request looked idle and was evicted,\nwith its rendition disposed, under the open response. Streams served from\na session are now wrapped by `StreamSession.Track`, and\n`EvictExpiredSessions` skips a session until they are all disposed.\n`SessionIdleTimeoutMinutes` now defaults to 10 instead of 3, so a paused\nHLS player that has stopped requesting segments survives a longer pause.\n\n`refactor` (`abstractions`): **Serve rendition resources for either delivery mode**:\n\nSplit the resource half of `IHlsPresentationRendition` into\n`IStreamRenditionResources`, which any rendition can implement to serve\nfiles beside its stream, such as subtitle tracks and fonts.\n\n- Renamed `HlsResource` and `HlsResourceRequest` to `StreamResource` and\n  `StreamResourceRequest`\n- `IHlsPresentationRendition` is now `IStreamRenditionResources` with\n  `master.m3u8` as its entry point\n- Added `Stream/Direct/{sessionID}/{**path}`, and `Stream/Hls/{sessionID}/{**path}`\n  now serves any rendition that implements the interface\n\n`feat` (`api`): **Describe stream sessions**:\n\nAdded `IStreamRenditionResources.DescribeAsync`, so a rendition can tell\nclients what it offers beyond the stream, and `Stream/Sessions/{sessionID}`\nto serve it.\n\n- `StreamDescription` lists video and audio tracks (ordinal, codec,\n  language, dimensions or channels, default, per-user rank), subtitles,\n  attachments, chapters and extras, plus rendition-specific `Metadata`\n- The endpoint resolves every resource path to a URL carrying the\n  request's query string, and reports the session's transform and\n  delivery mode\n- The stream redirects, the session manifest and the progressive stream\n  now send `Link: <...>; rel=\"describedby\"` pointing at it\n\n`feat` (`streaming`): **Resume evicted stream sessions under the same id**:\n\nA client returning to a session after it was evicted, such as a player\nresumed after a long pause, got a 404 and playback stopped. Sessions\nstarted through `Stream/Hls/master.m3u8` or `Stream/Direct` now record\ntheir video, transform and query string, and a request naming an evicted\nsession within `EvictedSessionResumeHours` (default 24) rebuilds the\nrendition under the same id, once per session however many requests\narrive together. The session's cache directory is no longer named after\nits id, so a rebuild cannot race the deletion of the old one.\n\n`chore` (`abstractions`): **Bump version to `6.0.0-alpha.88`**.\n"
    },
    {
      "version": "6.0.0.467",
      "tag": "v6.0.0-dev.467",
      "commit": "590863c680839b5e0df823bb02beafb5c186ed1b",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.467/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-15T21:59:55Z",
      "releaseNotes": "\n`fix` (`import`): **Fall back to the episode watched record when transferring state**:\n\nSetWatchedStateIfNeeded only copied watched state from still-existing\nsibling files, so replacing a watched release (e.g. TV with blu-ray)\nafter the watched copy was deleted left the new file unwatched, even\nthough the episode-level record still carried the state.\n\n- Add an episode-level AnimeEpisode_User fallback (newest watched date\n  and playback count) under UseExistingFileWatchedStatus\n- Skip users whose new file is already watched to avoid overwrites\n"
    },
    {
      "version": "6.0.0.466",
      "tag": "v6.0.0-dev.466",
      "commit": "fb254f8ad7bbee03e2292a4285b7c9acc32cf293",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.466/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-15T18:20:04Z",
      "releaseNotes": "\n`fix` (`anidb`): **Read `code` attribute from `<error>` in mylist response** ([#1440](https://github.com/ShokoAnime/ShokoServer/pull/1440)):\n\nAniDB's error element uses `code`, not `value`. The wrong attribute\nname made `Attribute(\"code\")!` throw `ArgumentNullException` on any\nerror response (e.g. `<error code=\"310\">illegal input or access\ndenied</error>`), masking the real error and crashing MyList refresh.\n"
    },
    {
      "version": "6.0.0.465",
      "tag": "v6.0.0-dev.465",
      "commit": "0e6d2dac2abd3304b8f5532d6ebd3479f6068ac3",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.465/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-15T18:11:49Z",
      "releaseNotes": "\n`misc`: **Some Stream Pipeline Fixes and Tests**.\n"
    },
    {
      "version": "6.0.0.464",
      "tag": "v6.0.0-dev.464",
      "commit": "c6e1734209c654800bc280015a789a7b715f7171",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.464/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-15T17:57:09Z",
      "releaseNotes": "\n`fix` (`db`): **Tolerate quoted identifiers when patching stored SQLite DDL**:\n\nA tool that rebuilt a table outside Shoko, such as DB Browser for SQLite,\nstores the CREATE TABLE with every identifier double-quoted in\n`sqlite_master`. Migrations that tighten a column's nullability or retype\nit patch the stored blueprint in place, and the patch regexes could not\nsee past the quoting, so migration `164.1` aborted on such a database\nwith `Could not find a definition for Title` and left it blocked.\n\n- Accept an optional `\"` pair around the column name in\n  `NotNullVariantOf` and `RetypedVariantOf`, keeping the quotes in the\n  rebuilt DDL\n- Pin the affected user's exact table shape in regression tests for both\n  paths\n"
    },
    {
      "version": "6.0.0.463",
      "tag": "v6.0.0-dev.463",
      "commit": "78e0390ffebba4e5991bdc003ee2cbb70bf5d1e7",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.463/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-15T02:31:40Z",
      "releaseNotes": "\n`misc`: **Bump abstractions**.\n\n`fix` (`anilist`): **Decompress responses from the `Anilist` http client**:\n\nThe `Anilist` client advertised gzip, deflate and brotli via\n`Accept-Encoding` but had no handler with `AutomaticDecompression`, so\nbrotli-encoded GraphQL responses were handed to `JsonNode.Parse` still\ncompressed and failed with \"'0x1B' is an invalid start of a value\".\nGave it the same `SocketsHttpHandler` setup as the `Default` client.\n"
    },
    {
      "version": "6.0.0.462",
      "tag": "v6.0.0-dev.462",
      "commit": "1b186c7da80f84e28f663ad755708253802e607a",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.462/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-15T02:25:54Z",
      "releaseNotes": "\n`fix` (`relocation`): **Resolve the managed folder path before containment checks**:\n\nThe containment check compared a fully-resolved path against the raw\nfolder path, which never matches on Windows where GetFullPath adds a\ndrive letter. Resolve the folder path once and use it for the\ncomparison and downstream relative-path math. The source-location test\nasserts against the folder's platform-normalized path so it holds on\nboth platforms.\n"
    },
    {
      "version": "6.0.0.461",
      "tag": "v6.0.0-dev.461",
      "commit": "dcf5e5eea2d955c04c40ec03e62eed725678aee6",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.461/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-15T02:11:52Z",
      "releaseNotes": "\n`misc`: **Plugin Stream Pipeline**.\n\n`misc`: **Plugin Stream Pipeline**.\n\n`misc`: **Update**.\n\n`merge`: **Merge branch 'plugin-stream-pipeline'**:\n\n# Conflicts:\n#\tShoko.Server/Plugin/PluginManager.cs\n#\tShoko.Server/Services/SystemService.cs\n\n`merge`: **Merge remote-tracking branch 'origin/master'**.\n"
    },
    {
      "version": "6.0.0.460",
      "tag": "v6.0.0-dev.460",
      "commit": "1b89548c6f6e3909c1d2bf4f096980a007130ecf",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.460/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-14T23:48:32Z",
      "releaseNotes": "\n`refactor` (`abstractions`): **Expose `IAuthenticationThrottleService` to plugins and back it by settings**:\n\nExposed the authentication throttling to plugins through `IAuthenticationThrottleService`, so plugins that authenticate users, such as Forgotten for its password reset and username recovery flows, share the core's lockouts instead of keeping their own. Moved its policy from constants into server settings.\n\n- Added `IAuthenticationThrottleService` to the abstractions. It accepts only an `HttpContext`, a `HubCallerContext` or an `IUser`, so plugins can't lock out or reset arbitrary keys\n- Added `ThrottleAuthentication` to the interface. It checks a username that need not exist, so unknown and existing usernames get the same answer\n- Renamed `LoginThrottler` to `AuthenticationThrottleService` and moved it to `Shoko.Server.Services`\n- Kept the raw username overloads `internal` for `UserService`, and prefixed username keys with `user:` so they can't collide with `ip:` client keys\n- Added `AuthenticationThrottleSettings` under `Web.AuthenticationThrottle`, with ranges enforced by the settings validation and a check that the initial lockout doesn't exceed the max lockout\n- Cached the policy in `AuthenticationThrottleService` and refreshed it when the settings are saved; lowering the max lockout also shortens existing lockouts\n- Switched the login controllers to `IAuthenticationThrottleService` and removed the `LoginThrottlerExtensions` helpers\n\n`feat` (`api`): **Mark optional authentication in the OpenAPI document**:\n\nAdded `OptionalAuthenticationAttribute` for endpoints that accept authentication but don't always require it, and made `AuthorizeOperationFilter` list the API key as optional for them.\n\n- Emitted an empty security requirement next to the API key for marked endpoints that don't require authorization, including endpoints without any authorization attributes\n- Marked `/api/v3/Init/Status`, whose response adds details for authenticated callers\n- Marked the `/api/v3/File/{fileID}/Stream*` endpoints, which only let anonymous callers through when `AllowAnonymousFileStreamingInAPIv3` is enabled, so Swagger UI now sends the key to them\n\n`feat` (`api`): **Document the `apikey` query parameter in the OpenAPI document**:\n\nAdded an `ApiKeyQuery` security scheme for the `apikey` query parameter, which the authentication handler already accepted but the OpenAPI document never described.\n\n- Listed the header and query parameter schemes as alternative requirements wherever the API key is required or optional\n- Moved the scheme names into constants on `AuthorizeOperationFilter`, kept the existing `ApiKey` name for the header scheme\n\n`feat` (`abstractions`): **Add features for plugins to advertise to clients**:\n\nAdded `IPlugin.GetFeatures` so plugins can advertise what they add to the server, e.g. Forgotten advertising password reset so a login page knows to offer it.\n\n- Added `PluginFeature` with a lowercase kebab-case name, a `Version` defaulting to `1.0.0`, a `PluginFeatureVisibility` defaulting to `Authenticated`, and optional `JObject` metadata\n- Added `PluginFeature.IsValid`, which rejects names that aren't kebab-case or exceed `MaxNameLength`, missing versions, and undefined visibilities\n- Added `LocalPluginInfo.GetFeatures`, which drops invalid and duplicate features instead of correcting them, trims versions to major, minor, and patch, and keeps the order the plugin returned\n\n`feat` (`api`): **Add `/api/v3/Plugin/Features` endpoint**:\n\nAdded an endpoint for clients to discover the features advertised by the active plugins, before or after signing in.\n\n- Returned only anonymous features to unauthenticated callers, also authenticated features to users, and every feature to administrators\n- Included the plugin ID on every feature, and the full plugin info only for administrators, so anonymous callers can't fingerprint installed plugin versions\n- Ordered features by plugin load order, then in the order each plugin advertised them\n- Logged each dropped feature once per run, and skipped the features of a plugin that threw instead of failing the whole response\n\n`chore` (`abstractions`): **Bump abstractions version to v6.0.0-alpha.86**.\n"
    },
    {
      "version": "6.0.0.459",
      "tag": "v6.0.0-dev.459",
      "commit": "cf259054dbea286021adbe918a9ec7404a91fa28",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.459/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-14T21:19:01Z",
      "releaseNotes": "\n`feat` (`abstractions`): **Add models and a facade service for release management**:\n\nExpose the release management system to plugins via a new\nIReleaseManagementService facade over the internal grouping, comparison,\nand auto-management services, with mapped abstraction models\n(SeriesWithCandidates, ReleaseCandidate, ReleaseOverride,\nReleaseDeletionPreview). Move ReleaseVersionStrategy to the abstractions.\n"
    },
    {
      "version": "6.0.0.458",
      "tag": "v6.0.0-dev.458",
      "commit": "9333e47c5b10fc0152ae7b37c65f427b808bb2ff",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.458/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-14T18:41:11Z",
      "releaseNotes": "\n`repo`: **Revert fork-only tooling commits that leaked in via #1426** ([#1439](https://github.com/ShokoAnime/ShokoServer/pull/1439)):\n\n* Revert \"repo(workflows): bump codeql-action to v4\"\n\nThis reverts commit 056b553dbc0345a2789423e9384012a8bd993d42.\n\n* Revert \"Add workflow_dispatch trigger to CodeQL\"\n\nThis reverts commit f09452e395369dba1ee0e9c37cc8d7d276cca47d.\n\n* Revert \"Add Dependabot and CodeQL to fork tooling\"\n\nThis reverts commit 815009a2dc187620e9d1a00260235cf95391c903.\n"
    },
    {
      "version": "6.0.0.457",
      "tag": "v6.0.0-dev.457",
      "commit": "462734a1acef5cf2cd53e5468afa022b96397957",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.457/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-14T16:53:28Z",
      "releaseNotes": "\n`misc`: **Add Dependabot and CodeQL to fork tooling**:\n\nDependabot covers NuGet + GitHub Actions weekly. CodeQL scans C# on\npush/PR to master plus a weekly schedule. Fork-local only, not for\nupstream PRs.\n\n`misc`: **Add workflow_dispatch trigger to CodeQL**:\n\nLets the workflow be run on demand for verification, without waiting\nfor the weekly schedule or a push.\n\n`repo` (`workflows`): **Bump codeql-action to v4**:\n\nv3 forces Node 20 actions onto the runner's Node 24, which GitHub\nflags as deprecated. v4 targets Node 24 natively.\n\n`feat` (`auth`): **Add password-reset file on startup and harden API login**:\n\nAdd a startup password recovery path: a password-reset.json file in the\ndata folder resets the named user's password, invalidates their API\ntokens, and the server exits so it can be restarted with the new\ncredentials. The file is kept when the user is not found so it can be\ncorrected and retried; malformed files are ignored so they cannot block\nstartup.\n\nHarden login with per-username and per-client throttling (escalating\nlockout, 429 with Retry-After on the v0/v3 login endpoints), constant\ntime password hash comparison, and logging of failed attempts.\n"
    },
    {
      "version": "6.0.0.456",
      "tag": "v6.0.0-dev.456",
      "commit": "375462e2f11abf3a46b817477e2666685a10c006",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.456/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-14T16:48:53Z",
      "releaseNotes": "\n`fix` (`abstractions`): **Make `LanguageExtensions` unknown-language tracking thread-safe** ([#1426](https://github.com/ShokoAnime/ShokoServer/pull/1426)):\n\n`GetTitleLanguage()` mutated an unsynchronized static `HashSet<string>`\nto dedupe unknown-language reports. Concurrent callers (e.g. API\nrequests building File DTOs) corrupted it and crashed with\n`InvalidOperationException`.\n\nReplaced the `HashSet` with a `ConcurrentDictionary` used as a set, so\nno external synchronization is needed at any call site.\n"
    },
    {
      "version": "6.0.0.455",
      "tag": "v6.0.0-dev.455",
      "commit": "f8d3f928e06d3fed856106d0d103484be91a2085",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.455/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-14T04:37:01Z",
      "releaseNotes": "\n`fix` (`db`): **Declare the AniList text columns as utf8mb4 on MySQL**:\n\nThe schema parity job failed on 26 AniList text columns that MySQL\nreported as nullable while SQLite and SQL Server had them `NOT NULL`.\nThe tables declared their text columns with the old `UTF8` character\nset, so the after-patch charset fix rebuilt every one of them with a\nbare `MODIFY`, which drops anything left unstated, the null constraint\nincluded. Declared them `utf8mb4` with the unicode collation up front,\nmatching the repaired columns from v185, so the fix leaves them alone.\n\nVerified with `scripts/compare_schemas.sh` against MariaDB and SQL\nServer: all twelve parity tests pass.\n"
    },
    {
      "version": "6.0.0.454",
      "tag": "v6.0.0-dev.454",
      "commit": "e66c754070c943176ade4180e202f61bde8bfe4a",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.454/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-14T04:27:22Z",
      "releaseNotes": "\n`fix`: **Count missing TMDB episode links against the AniDB episode id**:\n\n`FilterableAnimeSeries.MissingTmdbEpisodeLinks` and the group\ncounterpart collected the linked AniDB episode ids and then looked up\neach episode's Shoko id in that set, so the count was wrong whenever the\ntwo id spaces diverged. Compared against `AniDB_EpisodeID`, and left out\nthe placeholder cross-references that mark an episode as unmatched.\n\n`fix` (`abstractions`): **Make the missing TMDB episode links selector read its own count**:\n\n`MissingTmdbEpisodeLinksSelector` returned `MissingEpisodes`, the count\nof missing episode files, instead of `MissingTmdbEpisodeLinks`.\n\n`fix` (`tmdb`): **Log the AniDB anime title instead of the title object**:\n\n`AniDB_Anime.PreferredTitle` is the `ITitle` object, so the episode\nmatching log lines printed the type name of `AniDB_Anime_Title` where\nthe anime title should have been. Switched them to `AniDB_Anime.Title`.\n\n`fix` (`api`): **Route the TMDB episode cross-references by episode id**:\n\n`GET /api/v3/Episode/{episodeID}/TMDB/Episode/CrossReferences` declared\nits route parameter as `seriesID`, although it looks the value up as a\nShoko episode id. Renamed the parameter to match the sibling endpoints.\n"
    },
    {
      "version": "6.0.0.453",
      "tag": "v6.0.0-dev.453",
      "commit": "071b3b0949212cd74cdb486a0fa31dc4fce40c11",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.453/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-14T03:50:45Z",
      "releaseNotes": "\n`refactor` (`tmdb`): **Share the air-date scoring helpers with other providers**:\n\nMoved `CalculateAirDateProbability`, `CalculateAirDateDistance` and the\nfallback distance cap out of `TmdbLinkingService` into a new\n`EpisodeMatchingUtility`, so every provider's episode matcher scores air\ndates the same way. No behaviour change for TMDB.\n\n`feat` (`anilist`): **Add the anilist metadata provider**:\n\nPorted the AniList integration shelved in April onto the current\nimage manager and queue, and finished the parts the original left\nunimplemented.\n\n- Metadata service with quick and full refresh, entity locks, a\n  sliding-window rate limiter that follows AniList's quota headers,\n  pauses the AniList jobs on 429 and 5xx responses, and traces every\n  call like the TMDB client does\n- Episodes synthesised from the airing schedule, with the schedule id\n  stored separately and the air time kept in UTC; episode ids are\n  packed as `(animeId << 12) | episodeNumber`\n- Linking service mirroring `TmdbLinkingService`, with the new\n  `DateAndNumberMatches` and `DateOffsetMatches` ratings; episode\n  numbers only ever corroborate a date match\n- Tags, studios, relations, characters, staff, images, external links\n  and the trailer, all exposed as resources and through the\n  abstractions; the main title is typed by its transcription language\n  and the preferred title follows the title language order\n- APIv3 controller with the TMDB-shaped include model, search,\n  refresh with quick mode, series-level linking, image download,\n  export and import with comments, plus the filter expressions and\n  selectors\n- Database tables for the three backends, settings, actions, recurring\n  purge, and the WebUI-facing 502/503 handling for upstream failures\n\n`fix` (`abstractions`): **Add Faroese, Twi, Chamorro and Nauruan to `TitleLanguage`**:\n\nTMDB tags some titles, overviews and images with the `fo`, `tw`, `ch`\nand `na` language codes, which `GetTitleLanguage` did not recognise,\nso every startup logged an unrecognised-language error for each of\nthem. Added the four languages to the enum and to the code-to-language,\nlanguage-to-code and display-name mappings.\n\n`feat` (`abstractions`): **Expose the performance language on cast and crew roles**:\n\nAdded `Language` and `LanguageCode` to `ICast` and `ICrew`, and a\n`Language` field on the API `Role` model. AniDB and TMDB only carry the\noriginal-language cast and crew, so their roles report the work's\noriginal language: TMDB from the show or movie's original language,\nAniDB from the language of the main title, resolved from its\ntranscription with the new `GetSpokenLanguage` helper. AniList lists\nthe cast and crew of every dub, so its roles report the language the\nperson works in: for the cast the voice actor's language, for the crew\nthe language qualifier AniList appends to dub staff roles (\"ADR\nDirector (English)\"), which is stripped from the forwarded role name,\nfalling back to the anime's original language when none is appended.\n\n- `GetTitleLanguage` now also resolves english language names, with\n  the few AniList uses that the enum-name fallback could not\n- Added `TryGetTitleLanguage`, a probe that does not report\n  unrecognised strings, so qualifiers such as \"(OP)\" stay out of the log\n\n`feat`: **Estimate the air time of episodes AniList has no schedule for**:\n\nAniList's airing schedule gives a precise broadcast time, but only for\nthe episodes it has scheduled. AniDB has a date for nearly every\nepisode, including future ones, and already reflects breaks and delays.\nThe shoko episode now combines the two: when an episode has an AniDB\ndate but no linked AniList schedule entry, its air time is the AniDB\ndate, taken as midnight UTC, plus the broadcast time learned from the\nseries' other linked episodes.\n\n- `AirTimeUtility.LearnAirTimeOffset` takes the median offset over the\n  ten most recent aired normal episodes with a known time, so a\n  long-running series follows its current slot and a one-off special\n  or delay does not skew it; fewer than two samples yields nothing\n- `AnimeSeries.AnilistAirTimeOffset` caches the learned offset, reset\n  on link and schedule changes and self-healing after an hour\n- `AnimeEpisode` applies it in `AirDateWithTime` for normal episodes and\n  reports it through the new `IShokoEpisode.IsAirTimeEstimated`\n- `IEpisode.AirDate` on the shoko episode is now the UTC calendar day\n  of `AirDateWithTime`, so the two never disagree, and a linked series\n  takes its days from AniList's broadcast schedule rather than an AniDB\n  date that records an early screening\n- The AniDB and AniList entities are untouched, and the episode\n  matchers keep reading those entities, so estimates never feed matching\n\n`fix` (`core`): **Keep the database blocked after a failed startup**:\n\n`DatabaseUnblockLoop` waited on the blocking tasks with `WaitAny`, which\nreturns for a faulted task the same as for a completed one, so when\nstartup failed and the startup task was faulted the loop removed it,\nfound nothing else blocking, and lifted the block. `IsDatabaseBlocked`\nturned false, the `[DatabaseRequired]` acquisition filter let every job\nthrough, and `WaitForDatabaseUnblockedAsync` resolved successfully, all\nagainst repositories that were never loaded. A plugin's recurring sweep\njob hit a `NullReferenceException` in `BaseCachedRepository` eight times\nthis way.\n\nA faulted or cancelled blocking task now keeps the block up and fails\nthe waiters with the same exception instead.\n\n`fix` (`core`): **Halt the queue for good when startup fails**:\n\nThe queue's hosted services are up before the database is, so after a\nfailed startup the orchestrator kept dispatching whatever the acquisition\nfilters let through, and a client could resume it through the API or\nSignalR at any time. Nothing in a failed process can serve a job.\n\n- Added `Halt` to the orchestrator and `IQueueScheduler`: a pause that\n  `Resume` cannot undo for the rest of the process lifetime, with\n  `IsHalted` alongside `IsPaused`\n- `WorkerPoolManager.Resume` no longer announces a start on a halted queue\n- `SystemService` halts the queue when `StartupFailedException` is set\n\n`feat` (`api`): **Put the broadcast time and filters on the calendar episodes**:\n\n`Dashboard.Episode` took its air date straight from the AniDB episode,\nso the calendar never saw the times AniList provides or the estimates\nthe shoko episode derives from them. It now reads the shoko episode\nwhen there is one.\n\n- `AirDate` is the shoko episode's day, the UTC calendar day of the\n  broadcast when the time is known\n- Added `AiredAt`, the UTC air time, set to midnight on `AirDate` when\n  no time is known, with `HasAirTime` and `IsAirTimeEstimated` flags so\n  a client can tell a real time from the placeholder\n- `CalendarEpisodes` fetches a day either side of the requested range\n  and filters on the final day, since a late-night slot can move an\n  episode across midnight UTC, and orders by the air time\n- Added `includeWithAirTime`, an `IncludeOnlyFilter` on the known\n  broadcast time, and a comma-delimited `type` filter on the AniDB\n  episode type that defaults to normal episodes\n- A series counts as missing when it has no files, not merely when\n  Shoko has no entry for it; the series and shoko episode stay attached\n  so the card keeps its link and broadcast time\n\n`chore`: **Sort usings and drop unused ones**:\n\nRan `scripts/sort_usings.py --all --strip-bom --remove-unused`. Only\nusing directives changed; nothing else in the touched files did. Covers\nthe AniList work from this branch as well as the AniDB connection-layer\nfiles from the earlier upstream restructure.\n\n`chore` (`abstractions`): **Bump abstractions version to v6.0.0-alpha.85**.\n"
    },
    {
      "version": "6.0.0.452",
      "tag": "v6.0.0-dev.452",
      "commit": "e967bfcd7024c0619d1530b3b7e457c7ea0dd55c",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.452/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-13T23:45:24Z",
      "releaseNotes": "\n`fix` (`tests`): **Make the shared fixtures in `Shoko.TestData` loadable**:\n\nNeither accessor on `TestData` worked, and nothing consumed them closely\nenough to notice.\n\n- `CrossRef_File_Episode` read the `AniDB_Anime.json` resource, so it\n  deserialised anime rows into cross-references and handed back records\n  whose every field was left at its default\n- `AniDB_Anime` threw outright, because `AniDB_Anime.AirDate` is a\n  `PartialDateOnly` and the stored `\"2024-07-03 00:00:00\"` has no default\n  Newtonsoft conversion. `Shoko.Benchmarks` is the only consumer, which is\n  why this stayed hidden\n\nMoved `PartialDateOnlyConverter` into `Shoko.TestData` so the fixtures and\n`FilterTests` share one copy, and added tests over both accessors.\n\n`test`: **Cover auto-grouping, the poco cache, episode lists and episode input**:\n\nAdded 102 tests over logic that had none, all of them free of a database,\na DI container and the network.\n\n- `AutoAnimeGroupCalculator` — the relation-graph walk and fuzzy title\n  metric that decide how a collection is carved into groups. Covers both\n  main-anime strategies, every `AutoGroupExclude` flag, transitive and\n  cyclic graphs, and the title normalisation, including that `the movie`\n  and `the animation` are stripped before comparison and that `-` splits\n  words where other punctuation does not\n- `PocoCache`/`PocoIndex` — the substrate behind every cached repository.\n  Covers index maintenance across add, update, remove and clear, and the\n  many-valued index\n- `AnimeSeriesService.EpisodeList` — the part matching that decides when a\n  split OVA or movie counts as held\n- `ModelHelper.GetEpisodeNumberAndTypeFromInput` — the `S3`/`C1` prefixes\n  the v3 API accepts in range parameters\n- `AnimeEpisode.DefaultTitle` — the English fallback and its placeholder\n\nAdded `Shoko.Tests/Infrastructure/` to reach code that reads `RepoFactory`\nwithout standing up a database. `CachedRepo` seeds a real repository's\n`PocoCache` and runs its own `PopulateIndexes`, so reads execute exactly as\nin production, and `RepoFactoryScope` installs those repositories into the\n`RepoFactory` statics and restores them afterwards. Those statics are\nprocess-global, so such tests join a non-parallel collection. Nothing here\ntouches the write-once `ISystemService.StaticServices`.\n\nAlso dropped `Startup` and `TestServerSettings`, both unreferenced —\n`Startup` is the `Xunit.DependencyInjection` convention class and that\npackage is long gone.\n\n`repo` (`workflows`): **Run the unit test suites in CI**:\n\n`integration-tests.yml` was the only workflow invoking `dotnet test`, and it\nruns `Shoko.IntegrationTests` alone. `Shoko.Tests` and\n`Shoko.QueueProcessor.Tests` had never gated a merge, so either could be\nbroken without CI noticing.\n\nBoth suites are self-contained — no database, no network and no native\ndependencies — so the job needs none of the setup the integration tests do\nand finishes in seconds.\n\n`test`: **Cover filter expression persistence and the sorting selectors**:\n\nTwo areas that had no coverage and fail quietly when they break.\n\n**Stored filter expressions.** `FilterPreset.Expression` and\n`SortingExpression` are persisted as JSON that records each node by its\n*simple* class name. `SimpleNameSerializationBinder.BindToType` resolves\nthat name by scanning loaded assemblies and taking the first match, and\nreturns `null` when nothing matches — which `FilterExpressionConverter`\nswallows through its error handler. So renaming or moving an expression\nturns every filter using it into a broken one, with nothing raised at the\ncall site, and two types sharing a simple name would bind to whichever the\nscan happened to find first.\n\nCovers all 284 concrete expression types: each binds back to its own type,\nno two share a simple name, and every constructible one survives a\nround-trip. Also pins that a name outside the `FilterExpression` hierarchy\ndoes not bind, which is what stops stored JSON naming arbitrary types.\n\n**Sorting selectors.** 76 of them, previously untested. Beyond evaluating\nto a non-null comparable, each is checked against the flags the filtering\nengine caches on: a selector that reads user info must declare\n`UserDependent`, and one whose value moves with the clock must declare\n`TimeDependent`.\n\n`FilterableFactory` populates the filterable doubles by reflection rather\nthan by hand, so a member added to the interface later cannot silently\narrive as null in tests that look like they cover it. `TestFilterable`'s\ntwo image-type properties gained `init` so they can be populated like every\nsibling.\n\n`refactor` (`relocation`): **Resolve file system access through `IFileSystemHelpers`**:\n\n`FileSystemHelpers` was a concrete class, so nothing that moves or deletes a\nuser's files could be exercised without a real disk. Extracted the interface\nit already implicitly had and switched every consumer to it. The concrete\ntype stays registered and the interface resolves to the same singleton, so\nbehaviour is unchanged.\n\nAdded tests over the guards in `DirectlyRelocateFile`, which had none. They\nrun against a mocked file system, and a rejected request is proven by the\nmock never being asked to move, delete or create anything:\n\n- requests with no managed folder, no relative path, or already cancelled\n- relative paths climbing out of the managed folder\n- files in an excluded folder, or in a drop destination when relocating\n  inside destinations is disabled\n- a source file that is missing on disk\n\nOne of these pins a subtle protection: containment is a prefix comparison,\nand it is only correct because `ShokoManagedFolder.Path` always ends in a\ndirectory separator. Without it a relative path could reach a sibling folder\nsharing a prefix — `/media/animeX` against `/media/anime` — and write\noutside the managed folder. Both that and the separator itself are now\ncovered.\n\n`test`: **Cover how video playback updates fold into stored user data**:\n\n`UserDataService` decides whether a file counts as watched, how far through\nit the user is, and how many times they have seen it — the state the whole\nwatched/unwatched view of a collection is built on — and none of it was\ntested.\n\nCovers marking watched and unwatched, the playback counter, progress\nhandling including the 97.5% \"near enough finished\" threshold, the\nsave/no-save decision, and the saved event.\n\nTwo of these pin behaviour that is easy to regress and invisible when it\ngoes wrong:\n\n- A watched date handed in as UTC is stored as local. The comparison against\n  the stored date is by value, so a UTC date would otherwise read as a change\n  on every write. Asserted on the `DateTimeKind` as well as the value,\n  because the value alone proves nothing when the tests run on a machine set\n  to UTC — which is what happens in CI.\n- An update carrying no changes must not write. Playback progress reports\n  arrive continuously, so a needless save there is a write per second per\n  client.\n\n`CachedRepo.BuildWritable` extends the harness to writes: a partial mock\nkeeps every real read path and replaces only the virtual `Save`/`Delete`,\nlanding them in the same cache the reads come from, so a saved entity is\nvisible to the next lookup as it would be in production. It also installs a\n`VideoLocal` in `RepoFactory` because `VideoLocal_User.ToString()` resolves\nthrough it, and Moq calls that when rendering a failed verification.\n\n`fix` (`db`): **Add the remaining missing primary keys on SQL Server**:\n\nSeven tables have no primary key on SQL Server while both SQLite and MySQL\ndeclare one:\n\n- `AniDB_Anime_PreferredImage`\n- `AniDB_Episode_PreferredImage`\n- `AniDB_FileUpdate`\n- `AuthTokens`\n- `ShokoImage_Entity`\n- `TMDB_Image`\n- `TMDB_Image_Entity`\n\nAll seven were created before the version 180 sweep that added the other 38\nbut were missed by it. Each keys off an `IDENTITY` column, so the values are\nalready unique and non-null and the constraint cannot fail against existing\ndata.\n\n`test`: **Compare the schema each database backend defines**:\n\nEvery backend carries its own copy of the schema as an ordered list of raw\nSQL, so the three can drift apart with nothing failing until a user on that\nbackend hits it. Both `add missing primary keys on SQL Server for 38 tables`\nand `widen TMDB_Show/TMDB_Movie Genres column on MySQL and SQL Server` were\nthat, and neither is reachable from a test that only exercises SQLite.\n\nReplays each backend's statements into a logical schema — tracking creates,\ndrops and renames — and asserts every surviving table declares a primary\nkey. This is what found the seven tables fixed in the previous commit.\n\nDeliberately narrow. A table-set comparison across backends was tried and\ndropped: distinguishing a genuine divergence from a rename this crude parser\nmishandles needs a real SQL parser, and a check that cries wolf is worse\nthan no check. The primary-key assertion needs only the table name and\nwhether the statement declares a key, which is reliable across all three\ndialects.\n\n`StubSettingsProvider` exists because the MySQL backend reads the settings\nsingleton while initialising its DDL fields, so one has to be installed\nbefore it can be constructed at all.\n\n`test`: **Cover automatic file deletion and the persistence converters**:\n\n**Automatic deletion.** `ReleaseAutoManagementService.ComputeRedundantPlaces`\ndecides which of a user's files get removed when a better release of the\nsame episodes is present. It is the most destructive decision the server\nmakes unattended and it had no tests. It returns the list rather than acting\non it, so every rule is checkable without deleting anything, and the real\n`ReleaseComparisonService` is used rather than a stub so the actual ranking\nruns.\n\nThe two that matter most:\n\n- A file belonging to the primary is never deleted, even when the same\n  physical file also appears in a secondary gap-fill candidate. Removing the\n  guard that enforces this makes both of those tests fail.\n- The eligibility gate holds. A primary that is mixed, corrupted, or has no\n  release info cannot cause deletions, and the bypass is reserved for a\n  primary the user picked by hand.\n\nAlso covers per-file versus whole-candidate mode for airing series, and that\na file whose episode coverage cannot be resolved is always kept.\n\n**Persistence converters.** The `IUserType` implementations sit between\nentity properties and columns, so one that loses information corrupts data\nsilently. Checks the contract every converter owes NHibernate across all\ntwelve — column types, returned type, and that two nulls compare equal,\nwhich dirty-checking runs on every flush — plus round-trips for MessagePack,\nstring lists, partial and whole dates, types and JSON.\n\nOne pins a limitation rather than a guarantee: `StringListConverter` joins\non `\"|||\"` with no escaping, so an entry containing that separator comes\nback as two entries. Better recorded here than discovered in someone's data.\n\n`test`: **Cover the missing-episode statistics written onto a series**:\n\n`AnimeSeriesService.UpdateStats` computes the counts behind the\nmissing-episode filters, the dashboard and the calendar, and nothing else\nrecomputes them — a series carries whatever this last wrote.\n\nCovers episodes with and without files, that un-aired episodes are not\ncounted as missing, that hidden episodes are counted separately, that only\nregular episodes count (not specials or credits), that the counts are\nrecomputed rather than accumulated across runs, and the derived\n`LatestLocalEpisodeNumber` and `LatestEpisodeAirDate`.\n\nMarked `AnimeSeriesRepository.Save(AnimeSeries, bool, bool)` and\n`AniDB_GroupStatusRepository.GetByAnimeID` as `virtual` so they can be stood\nin for. `virtual` alone changes no behaviour, and it follows what was\nalready done ad hoc for the handful of methods an earlier test needed.\n\n`refactor` (`anidb`): **Resolve the UDP socket through a factory**:\n\n`AniDBUDPConnectionHandler` constructed its `AniDBSocketHandler` inline, so\nnone of the protocol handling around the socket could be reached without\nbinding a port. `IAniDBSocketHandler` already existed; this adds the factory\nto go with it and switches the field to the interface. The real factory\nreturns the same socket handler as before, so behaviour is unchanged.\n\n`test`: **Cover the AniDB HTTP and UDP connection handlers**:\n\nNeither connection handler was tested, and both carry the logic that keeps\nthe server out of an AniDB ban — which costs a user a day of metadata when\nit goes wrong.\n\n**HTTP.** A successful call returns the body and status; a response\ncontaining the `>banned<` marker sets the ban and throws; a further call\nwhile banned never reaches the network, because talking to AniDB while\nbanned is what extends the ban; `force` still gets through for the caller\nthat needs it; and a server error or transport failure is surfaced rather\nthan mistaken for a ban.\n\n**UDP.** `Init` builds the socket and records whether it connected, and\nrefuses incomplete credentials. Commands go out as UTF-16 or ASCII as asked,\nreplies are decoded by their byte order mark and the mark is stripped. An\nall-zero reply is treated as a ban — a silent socket cannot be told apart\nfrom one, and assuming the worse is what stops the server digging deeper —\nafter which `Send` stops talking.\n\n**No test opens a connection of any kind.** HTTP goes through a stub\n`HttpMessageHandler` that answers from a queue, and UDP through a stub\n`IAniDBSocketHandler` that replays canned payloads. Both are pointed at\n`anidb.invalid` — reserved by RFC 2606 and guaranteed never to resolve — so\nanything that did try to reach the network would fail loudly rather than\nquietly succeed.\n\n`test`: **Cover parsing of the AniDB anime XML**:\n\n`HttpAnimeParser` turns AniDB's XML into the anime, title and episode\nrecords the rest of the server is built on. It is pure — XML in, objects out\n— and had no tests, in one of the most frequently fixed areas of the\ncodebase.\n\nCovers the documents it must reject (no anime id, no main title), the anime\ntype mapping, episode counts, the restricted flag, titles with their type\nand language, and the episode fields including type prefixes, double\nepisodes, and length in minutes becoming seconds.\n\nTwo pin AniDB quirks that are invisible until they break:\n\n- `1970-01-01` is AniDB's \"unknown date\" sentinel. Taking it literally would\n  file shows under 1970 and break every year filter and season grouping.\n- Apostrophes arrive as backticks throughout the API and are translated back\n  in titles and descriptions.\n\n`fix` (`db`): **Compare list-valued columns by value when checking for changes**:\n\n`StringListConverter`, `TmdbContentRatingConverter` and\n`TmdbProductionCountryConverter` all return a `List<T>`, and all compared\nwith `x.Equals(y)`, which for a list is reference equality.\n\n`NullSafeGet` builds a fresh list on every load, so the loaded value never\nmatched the stored one and NHibernate treated every mapped list column as\nchanged on every flush, rewriting rows that nobody had touched. The columns\naffected are the AniDB title/tag lists and the TMDB content ratings and\nproduction countries.\n\nThey now compare the string each side would be written as, so two lists are\nthe same exactly when they would produce the same column value.\n\n`test`: **Make the sorting selector and converter tests actually able to fail**:\n\nAn adversarial review of the new suites found several that passed regardless\nof what the production code did. Verified by mutation, before and after.\n\n**Sorting selectors.** 74 of the 75 are a single property read, so \"produces\na value\", \"is comparable\", \"is deterministic\" and \"ignores the time\" could\nnever be false; and the two value assertions could not tell one property\nfrom another, because `FilterableFactory` gave every property of the same\ntype the same sample. Changing a selector to return a sibling field kept all\n381 cases green.\n\nThe factory now seeds each value from the property name, and the selectors\nare pinned by a generated table of selector to the field it reads. Four\nselectors mutated to read a sibling — plain, nested, user-scoped, and the\ndate-converting one — now each fail.\n\n**Converters.** Both equality theories passed `x` as both arguments or two\nnulls, so only the `ReferenceEquals` short-circuit ran and the comparison\nthat decides whether a row is rewritten was never reached. Replaced with\ndistinct-but-equal values per converter, which caught the list-comparison\nbug fixed in the previous commit. Dropped the two that asserted a hardcoded\n`typeof()` was not null and one that asserted nothing at all, and added\n`MessagePackConverter<MediaContainer>` explicitly, since reflection never\nyields an open generic and it had been excluded from every theory.\n\n**AniDB HTTP.** `TheRequestGoesToTheConfiguredServer` only checked that the\nrelative URL it passed in came back, so the handler could ignore the\nconfigured server entirely and stay green. Asserts the whole URI now.\n\nAlso: assert the actual date rather than not-null in the series stats,\ntighten an over-broad `ThrowsAny`, drop a strictly weaker duplicate in the\nrelease management tests, and correct a class comment that claimed ranking\nwas exercised when only redundancy is.\n\n`test`: **Actually compare the schema across backends**:\n\nThe file was named for a cross-backend comparison but never made one —\nevery assertion ran against a single backend, so deleting a table from\nMySQL alone kept it green.\n\nTwo replay bugs had to be fixed first, or real drift would have been\nindistinguishable from parser noise:\n\n- A table rename now requires `RENAME TO`. The old pattern also matched\n  `ALTER TABLE x RENAME <col> TO <col>`, a column rename, which renamed the\n  table to the column name and left a phantom table behind.\n- Command strings holding several `;`-separated statements are now split.\n  Only the first was ever examined, so a create or drop following a rename\n  in the same migration was invisible.\n\nWith those fixed the backends agree on every table but `Language`, which all\nthree drop — SQLite from a coded migration that a static replay cannot\nobserve, so it is excluded and the reason recorded.\n\nRemoving one `CREATE TABLE` from MySQL alone now fails.\n\n`test`: **Record the empty episode-input crash as a skipped test**:\n\n`GetEpisodeNumberAndTypeFromInput` reads `input[0]` with no length check, so\nan empty string throws `IndexOutOfRangeException` instead of returning the\nerror tuple every other bad input gets. It is reachable from the v3 range\nparameters, where it surfaces as a 500 rather than a validation message.\n\nWritten against the intended behaviour and skipped with \"Possible bug -\nNeeds investigation\", so it documents what should happen and turns green\nwhen the guard is added, rather than asserting the current behaviour and\nenshrining it. Verified that adding the guard makes it pass.\n\n`test`: **Reach the release-group and air-date branches in the series stats**:\n\nThe review found three paths in `UpdateMissingEpisodeStats` that no test\ncould reach, two of them because the harness could not express the input.\n\n- Un-aired episodes were arranged with `AirDate = 0`, which AniDB means as\n  *no date*, not *the future*. That took the fallback to whether the series\n  had finished, and passed only because the harness left the anime's end\n  date unset. A spec can now carry a real air date, so the future-dated\n  comparison is exercised, and the unknown-date fallback is covered\n  separately in both directions.\n- The group-status list was always stubbed empty, which forces every aired\n  episode to count as released and leaves `MissingEpisodeCountGroups`\n  structurally zero. A spec can now attach a release group to a file, so the\n  \"released by a group I collect\" total is covered — along with an episode\n  no group has released yet, which should not count as missing at all.\n\nSetting `epReleased` to true, or `epReleasedGroup` to false, now each fail a\ntest.\n\nTwo things this shook out about the setup rather than the code: a release\nonly exposes its group once all four group fields are set, and two harnesses\nmust not be alive at once, since both install into the same `RepoFactory`\nstatics.\n\n`test`: **Make the selector test data prove it can tell selectors apart**:\n\nSeeding each value from its property name left one hole: every collection\nwas built with exactly one element, so every `.Count` read returned 1 and\nthe four selectors reading a collection count — audio languages, subtitle\nlanguages, custom tags, user tags — were indistinguishable from each other.\nSwapping any of them to a sibling collection kept the suite green.\n\nCollections are now sized from the name as well as filled from it, and a\nguard asserts that two selectors reading different fields never resolve to\nthe same value on the double. That is the property the whole table depends\non, so it is checked rather than assumed: returning every set to a single\nelement now fails the guard, and the two count selectors above now fail when\nswapped.\n\nSelectors that genuinely read the same field are allowed to agree — there\nare two, both reading `SortName`.\n\n`test`: **Skip the latest-local-episode assertion pending a race investigation**:\n\nThe test asserts the right result but fails roughly one full run in ten, and\nnever when its class runs alone — so it would have flaked CI and nowhere\nelse.\n\nThe cause is not the test. `UpdateMissingEpisodeStats` updates\n`latestLocalEpNumber` and `lastEpAirDate` from inside an `AsParallel` body\nwithout synchronising either, while every neighbouring accumulator in the\nsame lambda is explicitly locked. A lost update leaves a stale value on the\nseries, which drives continue-watching ordering and the calendar.\n\nSkipped rather than fixed in place, and recorded for investigation. Guarding\none of the two accumulators was tried and did not settle it, so the extent\nis not yet established.\n\n`test`: **Close the gaps a second review found in the new suites**:\n\n**Converter equality had no negative direction.** Every assertion was\n`Assert.True`, so replacing any converter's `Equals` with `=> true` passed\nthe whole suite — the mirror of the bug fixed in `999db949d`, and worse,\nsince NHibernate would then never write a changed column back at all. Added\nthe unequal-value direction, which now fails for both converters when their\ncomparison is stubbed out.\n\nThe table also skipped the converters that inherit whatever equality their\nentity type happens to have, including the two heaviest columns in the\nschema. `MessagePackConverter<MediaContainer>` had been added to discovery\nwith a comment about having been missed, then left out of the one test the\ncomment was about. Covered now, and the TMDB rows use non-empty lists, which\nthe empty ones could never have distinguished.\n\n**The `Language` exclusion was unconditional**, so it also hid MySQL or SQL\nServer failing to drop the table. Only SQLite migrates it from code, so only\nSQLite is excluded; stubbing out MySQL's `DROP TABLE` now fails.\n\n**Nothing checked the selector table covered every selector** — one was\nsilently absent, and anything added later would have been covered only by\n\"returns something comparable\".\n\nAlso: scalars and collection sizes now occupy disjoint ranges, after the\ncollision guard caught a `.Count` matching a scalar and then two scalars\nmatching each other; and the schema remarks no longer claim to catch a\ncolumn-width regression, which nothing there models.\n\n`test`: **Cover playlist DSL rejection, and record the broken extras syntax**:\n\nThe playlist DSL is user-supplied text from the v3 API, so how it turns bad\ninput away matters as much as what it accepts. Covers unknown and\nnon-positive group IDs, non-positive release group IDs, and entries carrying\nmore than a group and a release group.\n\nWriting these turned up that the documented `g<id>+<extra>` suffix cannot be\nparsed at all. Entries are split on `+` before the extras are looked for, so\nthe suffix is never found and survives as a separate sub-item, which is then\nrejected. `recursive` fares worst: it begins with `r`, so it is taken for a\nrelease group ID. None of the seven documented flags can be used.\n\nRecorded rather than fixed, with the four documented forms as a skipped\ntheory. Only the rejection paths are asserted here — once an entry parses\nthe service builds the playlist, which needs the full service graph.\n\n`test`: **Close the gaps a third review found**:\n\n**An empty playlist entry was not really covered.** The test asserted only\nthat no error was raised, which stays true with the guard it was named for\ndeleted — an entry producing nothing is discarded further down anyway. The\nskip is not observable from outside; what is observable is that the entry\nstill consumes a position, so the assertion is now on the error being\nattributed to the right index.\n\n**Four converters looked covered for equality but were not.** They appeared\nonly among the equal values, where `Assert.True(Equals(a, b))` is satisfied\nby a stuck-true implementation — the very failure the pair of theories\nexists to catch. Covered in both directions now, and a check asserts the two\ntables stay paired, so a converter cannot be added to one and forgotten in\nthe other.\n\nAlso corrected the diagnosis recorded for the playlist extras bug. The split\nconsuming the `+` is only half of it: `IndexOf(['+', ' '])` binds to the\nspan overload and searches for the two-character sequence `\"+ \"`, which\nnever occurs. Either correction alone still fails; with both, the parser\naccepts all four documented forms, which is now measured rather than\nassumed.\n\n`test`: **Assert the documented playlist extras syntax, skipped pending a fix**:\n\nReplaces the earlier placeholder with the six examples taken from\n`ParsePlaylist`'s own remarks, covering the group and series branches, and\nwritten against the intended behaviour so it turns green when the parsing is\ncorrected.\n\nThe entry is split on `+` before the extras suffix is looked for, so the\nsuffix survives as a separate sub-item and is rejected; `recursive` is taken\nfor a release group ID because it begins with `r`. This has never worked —\nthe first version split on `+` and then searched the result for `+`, which\ncould never match — so nothing regressed, and two years without a report\nsuggests the flags are unused.\n\nCorrecting the split alone is not enough: `IndexOf(['+', ' '])` binds to the\nspan overload and looks for the literal sequence `\"+ \"`, so `IndexOfAny` is\nneeded too. Both were measured.\n\n`test`: **Drop the playlist extras assertion**:\n\nThe documented `g<id>+<extra>` syntax cannot be demonstrated as a defect.\n`items` arrives through `[FromQuery]`, and a query string decodes `+` to a\nspace, so a client following the documentation sends `g5+recursive` and the\nservice receives `g5 recursive`. The literal `+` only survives if the client\npercent-encodes it.\n\nThat makes the suffix indistinguishable from a second sub-item once decoded,\nwhich is why the parser accepts either delimiter, and means the syntax is\nnot expressible over this transport rather than being mis-parsed. The\nremaining rejection tests are unaffected.\n\n`test`: **Serialise the stub settings provider install**:\n\n`Install()` was an unsynchronised check-then-act against the process-global\n`ISettingsProvider.Instance`. `AniDBHttpConnectionTests` writes `HTTPServerUrl` onto\nwhatever provider is installed after calling it, and `SchemaParityTests` installs from a\ndifferent, parallelisable collection — so both could see the static unset, both install,\nand the loser's settings be discarded.\n\nAlso tidied stray whitespace in `PocoCacheTests` and a missing trailing newline in\n`PlaylistParsingTests`.\n\n`fix` (`db`): **Hash list-valued columns the same way they are compared**:\n\n`999db949d` made `IUserType.Equals` value-based on `StringListConverter`,\n`TmdbContentRatingConverter` and `TmdbProductionCountryConverter`, but left\n`GetHashCode` hashing the `List<T>` reference. Two values NHibernate now considers\nequal therefore hashed differently, which breaks the contract its user-type caching\nrelies on.\n\nBoth sides now key off the serialized form, so equal values hash equally.\n\n`repo`: **Build `Shoko.TestData` in the Release solution configuration**:\n\nIt had a `Release|Any CPU.ActiveCfg` but no matching `Build.0`, so a solution build in\nRelease skipped it while still resolving references to it. Debug built fine, which is\nwhy nothing had noticed; a Release build of anything referencing it failed with\n`CS0234`.\n\n`test` (`db`): **Compare the three backend schemas column by column**:\n\nThe three backends each keep their own hand-written DDL, and nothing forced them to\nagree. `SchemaParityTests` compared tables and primary keys only, so a column added,\nwidened or made nullable on one backend and missed on another went unnoticed —\n`add missing primary keys on SQL Server for 38 tables` and\n`widen TMDB_Show/TMDB_Movie Genres column on MySQL and SQL Server` were both that.\n\nThe new comparison reads the catalog of a real database of each backend rather than\nreplaying the DDL, because a replay cannot see the whole migration: MySQL performs some\nof its through `PREPARE stmt FROM @sqlstmt`, and every backend has migrations written\nin C#. Nothing is committed — a recorded schema is a copy that falls behind the\nmigrations it claims to describe — so the dumps are produced at runtime and compared\nside by side.\n\n- `SchemaSnapshot` reduces a backend's catalog to a type family, declared width and\n  nullability per column, so the dialects can be compared at all\n- `SchemaSnapshotTests` writes the dump for the backend it just migrated\n- `SchemaTypeParityTests` compares the three, and skips rather than passes when they\n  are not all present\n- CI publishes each job's dump and adds a `schema-parity` job over all three\n- `scripts/compare_schemas.sh` runs the whole thing locally against Docker\n\nSQLite takes no part in the width comparison, having no declared widths, and its\n`INTEGER` is treated as `BIGINT`, which it is.\n\nThe comparison currently fails: 6 columns differ in type, 15 in width between MySQL and\nSQL Server, and 110 in nullability. It reports them in full so the list can be worked\nthrough.\n\n`test` (`db`): **Share one server bootstrap across the integration tests**:\n\nBoth test classes took `DatabaseMigrationFixture` as a class fixture, which is one\ninstance per class, so the second bootstrap hit the write-once\n`ISystemService.StaticServices` and failed with `The service provider has already been\nset`. Running either class alone passed, which is why it was not caught when\n`SchemaSnapshotTests` was added.\n\nMoved to a collection fixture, so the whole run shares one.\n\n`chore` (`db`): **Trim the schema comparison comments**.\n\n`fix` (`db`): **Hold every backend to the nullability its model declares**:\n\n`SchemaTypeParityTests` found 110 columns whose nullability differed between the three\nbackends. Every one of them backs a non-nullable model property — `string MainTitle`,\n`DateTime LastUpdatedAt` — so a null could never have been read back into one, and\nSQLite already agreed with the model on 93 of the 95 it could be checked against.\n\nMost of the MySQL half was one cause: `MySQLFixUTF8` and `MySQLFixUTF8MB4` rebuild every\ntext column with `MODIFY`, which replaces the whole column definition and silently drops\nany attribute left unstated. 83 columns lost `NOT NULL` that way, `AniDB_Anime.MainTitle`\namong them, despite its `CREATE TABLE` declaring it. Those two are versioned migrations\nthat have already run, so they are left alone and the columns are repaired instead.\n\n- MySQL v185 tightens 90 columns\n- SQL Server v183 tightens 25, mostly TMDB `CreatedAt`/`LastUpdatedAt`\n- SQLite v164 tightens `AniDB_Anime_Title.Title` and `VideoLocal.DateTimeCreated`\n\nEach fills its nulls before altering, since a stored null would fail the alter.\n\nSQLite cannot tighten a column in place, so `MakeColumnNotNull` rebuilds the table around\na `CREATE TABLE` patched from the one the database reports. It has to come from the\ndatabase: `MoveAnidbFileDataToReleaseInfoFormat` is a `PostDatabaseFix`, which runs after\nevery other command, so a database migrating in one pass still has the `CRC32`, `MD5` and\n`SHA1` columns that one migrating from an older version dropped long ago. Both shapes are\ncovered by `SqliteNotNullVariantTests`.\n\nVerified against all three backends, migrating from empty and, for MySQL and SQL Server,\nupgrading a database already at the previous version.\n\n`fix` (`db`): **Repair the columns only damaged when the database is pre-created**:\n\nThe v185 list was derived from a database Shoko had created itself, which uses\nutf8mb4_unicode_ci — so `MySQLFixUTF8MB4` had nothing to convert for six columns and they\nkept their `NOT NULL`. A database created outside Shoko keeps the server's default\ncollation instead, and the conversion then reaches those too.\n\nCI creates the schema through `MARIADB_DATABASE`, so it hit exactly that and reported six\ndivergences the local run could not: `AniDB_Episode.Rating`, `AniDB_Episode.Votes`,\n`AnimeEpisode_User.UserTags`, `AnimeSeries_User.UserTags`, `Versions.VersionType` and\n`Versions.VersionValue`.\n\nSince every text column is converted when none of them match, this is the whole of the\ndamage rather than another instalment of it. Verified against a database created the way\nCI creates it, both migrating from empty and upgrading one already at v185.\n\n`fix` (`db`): **Skip runtime-emitted assemblies when resolving a filter's type**:\n\n`BindToType` scans every loaded assembly, and `GetTypes()` throws\n`ReflectionTypeLoadException` on one that is still being written to. The throw reaches\n`FilterExpressionConverter.ConvertFrom`, whose error handler sets `Handled` and hands back\n`null`, so the filter comes back blank rather than failing.\n\nCI hit it on six of the 287 `EveryConstructibleExpressionSurvivesARoundTrip` cases, spread\nacross unrelated namespaces — whichever happened to be deserializing while a Castle proxy\nwas being emitted for a mock elsewhere in the run.\n\nNothing bound here is ever emitted at runtime, so skipping dynamic assemblies costs\nnothing. Reproduced by mocking distinct interfaces while scanning, which threw 32, 52 and\n40 times across three runs, and threw none once the assemblies were skipped.\n\n`test`: **Cover the binder's assembly scan, and stop the rate limiter test racing timers**:\n\n`SimpleNameSerializationBinderTests` reproduces the scan failing while assemblies are\nemitted alongside it; it fails on all three runs without the fix in the previous commit.\n\n`SlotsExpireAfterWindow_AllowsNewCalls` asserted that two calls finish within 200ms after\na 200ms window expires. `SegmentsPerWindow` is 10, so slots came back in 20ms slices —\nfiner than a loaded runner services its timers, and it measured 373ms in CI. Widened to a\n1000ms window so a slice is 100ms, with proportionally more slack.\n\n`fix`: **Skip runtime-emitted assemblies in every type scan**:\n\n`212e9a7d3` fixed one of these. `TypeStringConverter` had the same scan and failed the\nsame way — `ReflectionTypeLoadException: Could not load type 'Castle.Proxies.ITextStreamProxy'\nfrom assembly 'DynamicProxyGenAssembly2'` — so the fix belongs in one place rather than\nwherever the next failure happens to land.\n\nThirteen scans looked up job types, subtitle providers, filter expressions, AniDB request\ntypes and mapped entities through `AppDomain.CurrentDomain.GetAssemblies()`. None of those\nare ever emitted at runtime, so none of them need the assemblies that are, and\n`Assembly.GetTypes()` throws on one that is still being written to.\n\n`ReflectionUtils.ScannableAssemblies` now states that once. `Shoko.QueueProcessor` cannot\nreference it, so its one scan says the same thing locally.\n\n`test`: **Assert logic rather than elapsed time**:\n\nThe suite measured how long things took in seven places, which fails on a runner under\nload and says nothing about the code. Two did fail: the rate limiter measured 1933ms\nagainst a 500ms bound, and a `Speed Test` averaged 2911ms against a 600ms target.\n\n- `TmdbRateLimiterTests` now asserts what the limiter reports — `RemainingInWindow`,\n  `CallsInWindow` and `BackoffUntilTicks`. That a caller with no permit left is made to wait\n  is `SlidingWindowRateLimiter`'s job; ours is to hand it the right window and record the\n  backoff, which is what those show. Mutation-checked: reporting full capacity, and never\n  recording a deadline, each fail two of them.\n- `TagFilterTest.TestSpeed` asserted an average under 2000ms and nothing else. That is a\n  benchmark, so it moved to `Shoko.Benchmarks` as `TagFilterBenchmarks`.\n- `ReflectionUtilsTests` covers the shared assembly scan; removing the guard fails it every\n  time, where the flake it replaces showed up about once in ten full runs.\n\nNo assertion on elapsed time is left in any of the three test projects.\n\n`test`: **Stop the last two tests depending on how the machine schedules them**:\n\n`TheScanSurvivesAssembliesBeingEmittedAlongsideIt` passed its cancellation token to\n`Task.Run`. A task that has not been scheduled by the time the token fires is returned\ncancelled rather than run, so `Task.WhenAll` threw `TaskCanceledException` on a runner with\nfewer cores than the eight tasks it starts. The token now only ends the loops. Confirmed by\nfiring it immediately: cancelled with the token passed, clean without it.\n\n`OnEnqueue_MaxBatchReached_TriggersImmediateFlush` slept 100ms and asserted a\nfire-and-forget flush had happened. It now waits to be told, with a timeout well clear of\nany scheduling delay. Still fails, as a timeout, when the force flush is disabled.\n\n`repo` (`workflows`): **Pin the artifact actions to the versions the repo already uses**:\n\n`upload-artifact@v4` and `download-artifact@v5` still run on Node 20, which GitHub now\nwarns about. Every other workflow here is already on `upload-artifact@v7` and\n`download-artifact@v8`.\n\n`fix` (`db`): **Keep the case-sensitive collation MySQL's text conversion undoes**:\n\nv170 made twelve columns `utf8mb4_bin` so that hashes, paths and tokens\ncompare case-sensitively. v185 rebuilt ten of them with `MODIFY`, which\nreplaces the whole definition, and every one of those named\n`utf8mb4_unicode_ci` — quietly making `VideoLocal.Hash`,\n`VideoLocal_Place.FilePath` and `ImportFolder.ImportFolderLocation`\ncase-insensitive again on any database upgrading through it. Each of the\nten now restates the collation the column already has.\n\n`MySQLFixUTF8` undid the same thing, and had been doing so since v170. It\nis a `PostDatabaseFix`, so on a database migrating in one pass it runs\nafter every patch, and it converted anything not already\n`utf8mb4_unicode_ci` — v170's columns included, dropping their `NOT NULL`\nalong the way. A fresh install therefore ended up case-insensitive where\none that upgraded step by step, having recorded the fix long ago, stayed\ncase-sensitive. It now leaves `utf8mb4_bin` alone, which is already\nutf8mb4 and never what it was written to convert.\n\n`SchemaTypeParityTests` cannot see a collation, so neither showed up\nthere. Verified on MariaDB migrating from empty: all twelve keep\n`utf8mb4_bin`, and no column is left on any other collation.\n\n`fix` (`db`): **Give SQLite the column types the other two backends declare**:\n\n`SchemaTypeParityTests` found six columns SQLite stores under a different\ntype family than MySQL and SQL Server do:\n\n- `AniDB_Anime.AirDate` and `EndDate` became `varchar(10)` on MySQL at\n  v171 and on SQL Server at v167, when they became a `PartialDateOnly`.\n  SQLite never got that migration and kept the `DATETIME` it was created\n  as, so rows written before the change still carry a time of day the\n  other two dropped\n- `AnimeEpisode_User.UserTags` and `AnimeSeries_User.UserTags` were added\n  by an `ALTER TABLE ... ADD COLUMN` that named no type at all, leaving\n  them with BLOB affinity where the model maps a `StringListConverter`\n- `TMDB_Episode.Runtime` and `TMDB_Movie.Runtime` map `RuntimeMinutes`,\n  an `int?`, but were created `TEXT`\n\nSQLite cannot retype a column in place, so v165 rebuilds each table the\nway v164 does for nullability. `RetypedVariantOf` patches the type into\nthe table's own `CREATE TABLE`, read from the database rather than\nwritten out here, and replaces only the type: the constraints that follow\ncarry the nullability, the default and the primary key. It has to cope\nwith a type that is several words, brackets a comma, or is not there at\nall, all of which `SqliteRetypedVariantTests` covers.\n\nVerified against all three backends migrating from empty.\n\n`fix` (`db`): **Declare one width per column across the three backends**:\n\n`SchemaTypeParityTests` found fifteen columns MySQL and SQL Server\ndeclare at different widths. In every case one of the two already bounds\nthe column and the other leaves it unbounded, so the bound is what the\ndata already is — nothing longer can ever have been written on the\nbackend that enforces it. Each takes the bound.\n\n- MySQL v186 narrows eight `text` columns SQL Server bounds:\n  `AniDB_Anime_Relation.RelationType` to 100, `ScanFile.Hash` and\n  `HashResult` to 100, `AnimeSeries.AirsOn` to 10, `FilterPreset.Name` to\n  250, and three user-entered titles to 500\n- SQL Server v184 narrows seven `MAX` columns MySQL bounds:\n  `AniDB_Episode.Rating` and `Votes` to 200, `AnimeSeries`'s two default\n  languages to 50, both `ImportFolder` columns to 500, and\n  `TMDB_Person.PlaceOfBirth` to 128\n\nEach trims its values to the width first, since anything longer would\nfail the alter. `FilterPreset.Name` drops its index before the alter and\nrecreates it after: MySQL indexed it by a 255-character prefix, which no\nlonger fits a 250-character column, and a bounded column does not need\none.\n\nVerified against all three backends, migrating from empty and, for MySQL\nand SQL Server, upgrading a database seeded with values too long for the\nnew width — every one is trimmed rather than failing the migration.\n\n`repo` (`scripts`): **Create the comparison's database the way CI does**:\n\n`compare_schemas.sh` created the MariaDB database with an explicit\n`utf8mb4_unicode_ci`, which is what Shoko's own `CREATE DATABASE` uses.\nCI creates it through `MARIADB_DATABASE`, which keeps the server default,\nand so does anyone who creates one outside Shoko — and only then does\n`MySQLFixUTF8MB4` reach the columns it leaves alone otherwise. The local\nrun therefore could not reproduce what CI reported. It now names no\ncollation either.\n\n`merge`: **Upstream automated-testing (unit test work)**.\n\n`merge`: **Upstream master (release fixes)**.\n\n`refactor` (`anidb`): **Restructure the connection layer around async requests**:\n\nReworked the AniDB provider so protocol handling no longer blocks on synchronous socket I/O, and split the state that was buried inside the connection handlers into first-class components.\n\n- Added `AniDbBanState` and `AniDbBanStateService`: idempotent bans, a `PeriodicTimer` expiry watchdog, and a single source of truth consulted by the API controllers, acquisition filters, and services\n- Extracted the UDP login/session protocol into `AniDbUdpSession` behind `IUdpSessionTransport`, with a reentrant async socket lock in the handler\n- Added `AniDbRateLimiter` as the shared base of `UDPRateLimiter` and `HttpRateLimiter`\n- Made `AniDBSocketHandler` and every UDP request async (`SendAsync`)\n- Async-ified the episode lookups in `VideoReleaseService` on top of the upstream invalid cross-reference counting\n- Adapted the upstream AniDB test doubles and connection tests to the new API, and silenced nullable warnings from the MessagePack generator\n"
    },
    {
      "version": "6.0.0.451",
      "tag": "v6.0.0-dev.451",
      "commit": "b5d101c295750b7793e46fa3bc17983852d0757e",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.451/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-13T21:16:47Z",
      "releaseNotes": "\n`chore`: **Correct argument name in video service**. (_Skip CI_)\n\n`fix` (`release`): **Allow a release to list the same episode more than once**:\n\n`CheckCrossReferences` inferred invalidity from a count difference, so\nevery deliberate collapse had to be compensated by hand. The `DistinctBy`\nthat merges identical percentage ranges never was, so a release listing\none episode twice threw `Release have 1 invalid cross reference(s)`\ninstead of saving. AniDB serves that shape whenever a file's related\nepisode points back at its own base episode.\n\n- Replaced the count reconciliation with an explicit `invalidXrefs`\n  count, raised only where a cross-reference is genuinely rejected — an\n  episode ID that is `<= 0`, or one AniDB answers `NO_SUCH_EPISODE` for\n- Normalised percentage ranges into the contract documented on\n  `IReleaseVideoCrossReference` rather than rejecting them: both bounds\n  are clamped before the range is reversed, so an out-of-range pair can\n  no longer come back out with the start past the end, and a zero-length\n  range is corrected to cover the whole episode\n- Kept the narrower-range override, so a related entry still replaces the\n  base entry's full range\n\n`refactor` (`release`): **Drop unused locals in CheckCrossReferences**:\n\nBoth locals were computed on every call and never read. `checkedIDs` once\nkept the method from asking AniDB about the same episode twice, a job\n`_unknownEpisodeIDs` took over. `fileName` walked the video's locations\nfor a string nothing consumed.\n"
    },
    {
      "version": "6.0.0.450",
      "tag": "v6.0.0-dev.450",
      "commit": "9b09cee78bc9de17be8b9946053536a3580ef522",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.450/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-13T04:49:00Z",
      "releaseNotes": "\n`feat` (`api`): **Report available and total bytes for `/api/v3/Folder/MountPoints`**:\n\nAdded `MountPointSizes`, which extends `ChildItems` with `AvailableBytes`\nand `TotalBytes`, and made it the type of `Drive.Sizes`. The values come\nfrom `DriveInfo.AvailableFreeSpace` and `DriveInfo.TotalSize` when the\ndrive is ready. `/api/v3/Folder` still returns the plain `ChildItems`.\n\n- `Drive.Sizes` hides `Folder.Sizes` with `new` and forwards to the base\n  property, so there is one backing value and the Swagger schema shows the\n  byte fields on `Drive` only\n"
    },
    {
      "version": "6.0.0.449",
      "tag": "v6.0.0-dev.449",
      "commit": "ea336025879e50969d1df71d8d846214464cb34a",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.449/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-12T22:14:27Z",
      "releaseNotes": "\n`fix` (`tmdb`): **Recompute series titles and overview after a link changes**:\n\nResetting a series' title or overview eagerly reloaded it, so a reset\nthat ran before the new TMDB rows landed re-memoised the old value and\nnothing invalidated it again until a restart. Drop the eager reload, warm\nexplicitly where a call site relied on it, and invalidate from the\nlinking service so movie links and the other callers are covered too.\n\nCloses #1424\n\n`fix` (`tmdb`): **Drop the series memo when an episode mapping changes**:\n\n`GetTmdbOverviews` reads the linked season and episode cross-references, so\nin the season-zero case a series' overview comes from an episode mapping\nrather than from the show, and changing one has to invalidate the memo.\n`ResetAllEpisodeLinks`, `SetEpisodeLink` and `MatchAnidbToTmdbEpisodes` all\nwrite those cross-references and none of them did. The last one resets after\nits save, which is past the early return for a preview, so a dry run still\nwrites and invalidates nothing.\n\n`fix` (`anidb`): **Drop the memoised titles when a refresh rewrites the rows**:\n\n`AniDB_Anime`, `AniDB_Episode` and `AnimeEpisode` memoise their default title\non first read, but the rows belong to `AniDB_Anime_Title` and\n`AniDB_Episode_Title`, which are served by repositories of their own, and the\nentities come from cached repositories that outlive the refresh. Writing the\nrows said nothing to the entity holding the memo, so a placeholder episode\ntitle stayed until a restart even though the real one had already arrived.\n\n- Added `ResetDefaultTitle` to the three entities, beside the field it clears.\n  None of them implement `IEntityMetadata`, so the `ResetAllTitles` seam the\n  TMDB entities use was not available here\n- Invalidated the episodes named by the title rows that were actually written\n  or removed, and the anime only when its own title rows changed, so the\n  decision is made from the write itself rather than a higher-level flag\n\n`fix` (`anidb`): **Assign the default title to its own field**:\n\n`AniDB_Anime.DefaultTitle` wrote both of its fallback results to\n`_preferredTitle`, the field behind `PreferredTitle`. That corrupted the\npreferred-title memo with a default title and left `_defaultTitle` null, so\nevery read for an anime without a main title re-ran the whole fallback,\nincluding the `AniDBTitleHelper` lookup. Only an anime with no\n`TitleType.Main` row reaches either branch, which is presumably why it went\nunnoticed. The first branch also memoised the `FirstOrDefault` result before\nits null check, so that is a pattern match now.\n\n`fix` (`shoko`): **Drop the memoised series default title when its inputs change**:\n\n`AnimeSeries.DefaultTitle` memoises the title override, falling back to the\nAniDB anime's own default title, and it was the last of the five title\naccessors with no way to clear the memo. The override paths cleared the\npreferred and anime titles but not this one, and the AniDB refresh cleared\n`AniDB_Anime`'s memo without touching the series, which caches the result of\nreading it, so a renamed series kept its old title until a restart.\n\n- Added `ResetDefaultTitle`, matching the three AniDB entities\n- Called it where an input actually changes: both override endpoints, v3 and\n  v1, and the AniDB refresh where the anime's titles were rewritten\n- Left the TMDB reset sites alone. The memo has no TMDB input, so clearing it\n  there would imply a dependency that does not exist\n\n`fix` (`tmdb`): **Only clear the series overview when an episode mapping changes**:\n\n`GetTmdbTitles` reads the linked show or movie and nothing below it, while\n`GetTmdbOverviews` descends into the season and episode cross-references, so\nan episode mapping can only move the overview. The episode paths were clearing\nthe preferred and anime titles as well, which recompute to the same value and\nimply an input they do not have.\n\n- Added `ResetSeriesOverview` beside `ResetSeriesTitlesAndOverview` and moved\n  the four episode paths onto it\n- Left the five show and movie paths as they were, since linking either one\n  changes `TmdbShows` or `TmdbMovies`, which the titles do read\n"
    },
    {
      "version": "6.0.0.448",
      "tag": "v6.0.0-dev.448",
      "commit": "ce6cb59284875cb9723ec64ff20b92ac79048a72",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.448/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-11T04:20:48Z",
      "releaseNotes": "\n`repo` (`deps`): **Bump `Shoko.BuildTools.Targets` to 0.3.0**.\n"
    },
    {
      "version": "6.0.0.447",
      "tag": "v6.0.0-dev.447",
      "commit": "f003434fe8bd80c07be710e8d4fb5299a91714e3",
      "downloadUrl": "https://github.com/ShokoAnime/ShokoServer/releases/download/v6.0.0-dev.447/Shoko.CLI_Framework_any-x64.zip",
      "date": "2026-09-11T04:04:52Z",
      "releaseNotes": "\n`feat` (`buildtools`): **Accept plugin metadata and dependencies without a manifest**:\n\n`shoko-build` gained `--id`, `--name`, `--overview`, `--tags` and\n`--dependencies`, and `Shoko.BuildTools.Targets` reads the matching\n`PluginID`, `PluginName`, `PluginOverview`, `PluginTags` and\n`PluginDependencies` MSBuild properties. A flag beats the property, which\nbeats the manifest; stated lists replace the manifest's instead of merging.\n\n- Dependencies are written as `<guid>@<range>[:optional|:required]`,\n  comma-separated, with whitespace around every separator ignored. The\n  same format is now embedded as `PackageDependencies`; the legacy\n  `guid:range[:true]` entries are still read.\n- An unparseable entry, a range the server cannot evaluate, or a plugin\n  listed twice stops the build, including in the manifest's `dependencies`.\n- `--id` has to match the manifest's `id` when one is being updated.\n- The server refuses to load a plugin whose embedded dependency list is\n  invalid, instead of skipping the bad entries.\n- `PluginDependencyList` is duplicated in `Shoko.BuildTools`,\n  `Shoko.BuildTools.Targets` and `Shoko.Server`.\n- Newlines and tabs in generated attribute values are escaped, so a\n  multi-line overview no longer breaks the build.\n- Bumped `Shoko.BuildTools` to 0.5.0 and `Shoko.BuildTools.Targets` to 0.3.0.\n"
    }
  ]
}
